# v1.0.8 部署记录

日期：2026-09-08。源码：`99a2eaf`；标签：`v1.0.8`；发布分支：`release/v1.0.8`。

## 制品与切换

- GitHub Actions 34236480999 成功构建。
- ARM64 固定镜像：`ghcr.io/taidai/zizu@sha256:80e6dcdbca6802a0946112691d2832bd4f0d46361cb78cfb68a75dac53908a9e`。
- 已替换应用容器，部署结果 `DEPLOYED_HEALTHY`，Schema 063 → 064；配置修订未改变。
- 保留 host 网络和 `/dev/mqueue` tmpfs；数据库、MQTT 与 Neuron 未重启。未启用策略、未下发控制。
- 首次临时容器提取 SQL 失败发生于停服和迁移之前；恢复原发布配置，改用现场既有 host/mqueue 参数后完成切换。

## 本次功能

- 调度选择、名称、触发方式和状态合并管理栏。
- 新增策略硬删除入口：一次确认；启用中或有未完成控制的策略拒绝删除。
- 关键事件与控制回读默认折叠。
- 策略删除语义见 `dispatch-strategy-delete.md`；独立控制审计、L0/L2 不随策略删除。

## 验证及边界

- 后端全量发现 779 项：494 执行通过、285 条件跳过。
- 本次真实 PostgreSQL/公共 API/仓库专项 33 项通过；Schema 检测 7 项通过。
- 本机合栏与双尺寸 3 项、删除及失败保留 2 项最终通过；前端构建通过。
- 发布前配置及有限控制快照已备份，隔离恢复到 Schema064 验证通过：60 张表、102 个外键。不是全量遥测恢复；临时恢复库已清理。
- 现场无头只读复验 10 项通过，覆盖首页、节点、L0、L1、L2、告警、设备、控制目录与调度；采集持续前进，业务写入、页面错误、API错误均为0。
- 可见浏览器刷新后 FE/API 均为1.0.8，确认合栏、删除入口和默认折叠。未真实删除现场策略，删除事务由本机专项验证。

## 容器日志清理

按用户授权清空全部5个容器的7个 Docker JSON 日志文件（含轮转文件），共169,186,335字节，约161.35MiB。清理后即时核对剩余0字节；运行后会继续产生新日志。旧日志不可恢复，未删除容器、卷、数据库或业务数据。

私有部署、备份、日志清理和浏览器证据位于本机 `.release-artifacts/v1.0.8/`，不提交凭据与现场参数。
