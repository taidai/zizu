# 1 号机 v0.5.1 HTTP 部署记录

日期：2026-08-31

## 发布身份

- 运行版本：`0.5.1`
- 运行功能提交：`cd86073d7cbdc41efeac1323c3ab5e959991bda7`
- 验收脚本后续修订：`9d92f4e`
- GitHub Actions：`33328038424`，成功
- ARM64 固定镜像：`ghcr.io/taidai/zizu@sha256:07261d665d1e4d73c42132cb71a67570c46997bca66158b1da3e9ccc8adb840b`
- 运行 image ID：`sha256:3f98f6c970f66ecbca46ebcd03a6c5e5d5402bde19467ec0bd68a13271b9ff142`
- Schema：`055`

`9d92f4e` 只改无头验收与测试资源清理脚本，不进入运行容器；运行功能代码与 `cd86073` 一致。

## 切换与回滚

- 仅拉取新固定摘要并重建 Compose `backend` 服务，TimescaleDB、NanoMQ、Neuron 和其他现场容器未重建。
- 保持 `network_mode: host`、`tmpfs: /dev/mqueue`、`restart: unless-stopped`。
- 切换前运行配置备份：`/opt/zizu-release-test-0.5.0/release.env.pre-v0.5.1-final-cd86073-20260831`。
- Schema 未变化；既有切换前数据库备份继续保留在 `/opt/zizu-backups/pre-v0.5.1-schema055-20260831/omnithings.dump`，此前已验证 SHA-256 为 `1f9c9f93d197359ee2b628cd3b88dbf6f10d8c8664dc60b0e36959c0bce91526`。

## 验证证据

- 后端完整测试：388 项，0 失败，152 项按条件跳过。
- 发布脚本测试：51 项，0 失败。
- 前端模型测试：55 项，0 失败。
- 前端生产构建成功；仅保留既有大 chunk 警告。
- 本地代理对 1 号机后端的节点主干验收：6/6 通过。
- 最终公网部署无头验收：6/6 通过，最终一次耗时 100.5 秒，0 跳过、0 重试。
- 页面旅程实际完成：登录；节点创建、编辑、搜索、刷新、选择和退役；Neuron 点位预览与导入；L0 实时、历史、筛选、分页、质量、时间和来源；L1 选择点位、试算、发布；L2 实时、历史、质量、来源和技术证据；共享模板保存；禁用且无动作的隔离规则绑定与解绑；读取失败可见并可恢复。
- 最终运行：`alive / 0.5.1`，Pipeline `RUNNING`，容器 `healthy`，restart 0，近 30 分钟 ERROR/CRITICAL/Traceback/PoolError 为 0。
- 根分区剩余约 3.8 GiB。

## 测试资源清理

- 保留唯一空测试边界根节点 `E2E验证`。
- 临时设备节点均已按产品“退役”语义处理。
- 活动 E2E 规则 0、E2E Neuron 节点 0、活动 E2E 共享模板 0。
- 早期测试产生的 11 个空重复根已精确退役；6 个 E2E 共享模板通过正式 API 发布“retired”修订，没有直接修改数据库。
- 验收脚本已修复节点加载竞态，并会自动退役本轮共享模板，后续运行不再累积这些活动测试资产。

## 边界

- 1 号机继续使用公网 HTTP 测试部署；未启动 Caddy、未申请或配置 TLS。
- 未执行自动策略、设备控制或真实设备写入。E2E 规则始终禁用且动作列表为空。
- 本轮证明共享模板可以从节点加工配置保存；模板目录中的选择、安装和版本维护尚未形成完整页面闭环，不能据此宣称共享模板全生命周期已经交付。
