# v0.8.0 HTTP 编辑修复与旧告警规则停用

2026-09-03，用户明确同意一起停用错误旧规则并部署修复。

## 发布状态

- 源码/tag：`531f13cd71c590b04a637ff170f1870641f5fad8` / `v0.8.0`。
- [Actions 33726683332](https://github.com/taidai/zizu/actions/runs/33726683332) 已成功；清单校验通过，1号机已切换并完成真实无头验收。
- ARM64：`ghcr.io/taidai/zizu@sha256:ac802413285523c8e740eb0650d0a15adf65e75ee7fe8ef523ab9740f725050d`。
- Schema060 不变。沿用已有单应用容器、host 网络及 `/dev/mqueue`，不启动代理或申请证书。
- 应用启动时间 `2026-09-03T07:30:29.661357076Z`，healthy、restart0；存活接口返回版本0.8.0。
- 数据库与NanoMQ未重启，启动时间分别保持 `2026-08-27T01:41:17.035605561Z`、`2026-08-27T01:42:27.816504869Z`。
- 发布后根分区剩余3.3G、使用率79%；Docker所在 `/userdata` 剩余3.2G、使用率74%。

## 现场规则处置

- 先只读复核目标：旧规则触发与恢复都为 false，未绑定通知；保留的生效规则 true 触发、false 恢复，绑定已启用且测试有效的渠道。
- 发现保留规则的最新保存草稿与生效定义不同：草稿不等于当前运行版本。本轮保留草稿和活动定义，不替用户发布草稿。
- 走公开的规则修订 → 计划 → apply 接口；预览严格检查仅一个实体的一项停用候选，没有新增或更新其他定义。
- 停用成功，配置修订616→617；旧定义的活动引用为0。保留定义、HTTP配置及绑定内容摘要未变。
- 告警和历史数据未删除；未把停用伪装成现场恢复，未执行确认/人工恢复、设备写入或历史消息补发。

## 本地验证

- 本轮完整后端516项，退出0、191项数据库条件跳过；scripts56/56、前端mjs88/88。
- v0.8.0生产构建通过，8195模块，既有大包警告保留。
- 生产包无头光标8项、通知选项4项、加载2项，共14/14，16.7秒；外部API模拟，没有发送现场消息。
- 既有独立审查已关闭旧ready计划绕过校验问题；仅版本号机械进位，未更换技术栈或增加依赖。

## 备份和回滚边界

- 变更前 `/opt/zizu-release-test-0.5.0/backups/v0.8.0-pre/` 保存私有release.env、Compose、runtime.env以及告警配置备份。
- `alarm-configs.dump`：108920字节、权限600、SHA256 `467c48f0403b2e2d8229359dab5b5fec4fa9646e296e3f63f5fa071e1671f712`。
  完整解码读取通过，未做隔离恢复。保留同日v0.7.8全库备份，无Schema变更，不重复慢速全库压缩。
- 软件回滚使用v0.7.9原镜像和release.env副本；不回滚数据库，也不重新启用已停用的错误规则。
- 如确需恢复旧规则，应通过新的配置发布显式决定；不能恢复整库覆盖此后的业务数据。

## 发布后验收

- 现场无头Chromium进程退出0：FE0.8.0；91/91原始点位正常，15V故障BIT保留数值0；L1已生效，2个L2实体实时值和质量正常。
- 当前/已恢复告警页面可用，列表HTTP200；旧规则已停用，正确规则仍启用；安全通知选项仅id/name/status，1项available，刷新通过。
- 系统工具中打开未保存的新HTTP草稿，验证中文选区替换、连续字段插入、焦点及后续键入均通过；没有保存草稿或发送测试通知。
  本机截图保留在 `.release-artifacts/v0.8.0/http-options.png`、`http-cursor.png`，不纳入源码提交。
- 现场公开试算接口：相同触发/恢复条件返回422 `ALARM_CONDITIONS_IDENTICAL`；true触发/false恢复的两个样本结果正确。
  试算不发布规则，也不产生通知；不能把它称为新真实故障通知验收。
- 停用完成后与部署验收后的配置相关6表内容摘要一致；旧规则活动引用为0，事件总数8256未增加，末次产生时间保持 `2026-09-03 07:09:25.371+00`。
  正确规则仍为true触发/false恢复，绑定渠道启用且测试摘要有效；未发布其另一份保存草稿。
- 内置Browser额外DOM抽查因自动安全审核连接中断而被拒绝，未绕过或重试；不将它计为通过。
  上述现场证据来自此前已经运行的独立无头验收，结束输出已核对，未因额外抽查失败重复操作。
- 未制造设备故障、强制恢复历史活动事件或补发消息；下一次真实通知的接收结果不在本轮证据范围内。
