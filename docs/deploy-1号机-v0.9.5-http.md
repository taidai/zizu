# 1号机 v0.9.5 发布记录

## 制品与范围

源码/标签：`v0.9.5` → `fb4b6e3e6f5ca07d14aa376f5bed7b7b9108c831`。仅修复调度试算：未编辑不隐式保存、已发布停用版本可以试算、阻断原因与输入快照展示、所见草稿摘要及一致性读取保护。Schema 仍为 `062`，没有新增依赖或数据迁移。

[GitHub 构建](https://github.com/taidai/zizu/actions/runs/34006759347)已成功，用时4分17秒，发布清单校验通过。ARM64镜像固定为：

```text
ghcr.io/taidai/zizu@sha256:33c0cb4ddb174d5bde4df25ca409e4b2625ddf52e074ff1e9d8347067633a58d
```

## 发布前验证

- 后端654项：396通过、258条件跳过、0失败，41.670秒；跳过项不计通过。
- 脚本56项、前端106项通过；生产构建通过（约1分钟，保留既有大资源包警告）。
- 无头调度界面11项通过（约1.3分钟），使用隔离API夹具，不代表真实设备回读通过。
- 修复阶段已有真实PostgreSQL并发及JDM/API专项验证和独立复核，详见[试算修复记录](reviews/2026-09-06-dispatch-trial-usability.md)。本轮未重复全部PG专项。
- 本地聚合报告当时为 `INCOMPLETE`，仅因尚未传入新版本现场地址；四项本地检查均为 `PASSED`。

## 备份与回退

备份目录：`/opt/zizu-release-test-0.5.0/backups/v0.9.5-pre-20260906T023108Z`。

52张配置表转储，672928字节，SHA-256为 `3e97737ac717acb8862bd91c1fdf7a12a7c018eccce564f5da5ea7aa4713921b`。关键配置、调度、控制及审计另做一致只读快照，在全新本机隔离库恢复60张当前表，验证102项外键；已移除的旧模板只归档，不重建。

关键快照SHA-256为 `e9df0ff9f7297cce0b0b0688cb68b9983e09fb3c93ef1faeb54535c216de2f91`，恢复报告与快照已归档至同一现场备份目录。此验证不包含全量L0/L2历史，也不是整站灾难恢复；隔离库未启动应用、未连接设备。

回退镜像为原 `v0.9.4`：`ghcr.io/taidai/zizu@sha256:45babfe419ccb04e9d057ef649b2f8e14fabc07eb7f65ac6b680669030812665`。确认没有活动控制或并发部署后，仅恢复发布环境并重建后端：

```sh
cd /opt/zizu-release-test-0.5.0
sudo cp -- backups/v0.9.5-pre-20260906T023108Z/release.env release.env
sudo docker compose -p zizu-release-test --env-file release.env -f docker-compose.test.e606.yml up -d --no-build --force-recreate --no-deps --wait --wait-timeout 100 backend
```

本次无Schema变更，回退镜像不应覆盖现场数据库或新配置。

## 部署与现场验收

2026-09-06 10:44（北京时间）切换完成，后端 `healthy`、重启计数0，首页与匿名存活接口均为HTTP 200，版本0.9.5。固定镜像摘要匹配，Schema062；部署后容器内调度API、运行时、仓储及统一控制模块源码摘要与本地一致。检查时部署后的44行日志没有ERROR/CRITICAL/Traceback，结论只覆盖该窗口。

只重建backend；数据库/MQTT容器身份、启动时间和重启次数不变，Compose/runtime散列及host网络、`/dev/mqueue` tmpfs等原配置不变，未发生回退。最终磁盘余量：系统盘约3.1GB，数据盘约3.4GB，未删除镜像或数据。

现网无头浏览器沿节点、L0实时/刷新/历史、L1当前加工和模板、L2实时/历史/来源、告警规则入口通过，约38.3秒（含健康查询）；页面异常及越界业务写请求为0。随后工作台、通知表格、HTTP通知配置和调度只读评估约29.5秒，页面异常为0。

现有停用草稿使用真实committed L2试算，HTTP200 / `EVALUATED`，显示输入快照和拟执行结果；未发生保存草稿请求，前后策略内容及发布/启用/故障锁状态完全一致。终验安全计数：启用策略、输出所有权、待执行意图、在途命令全0，历史控制命令总数未增加。

这不代表全部业务验收完成：补查5个L2时，3个旧实体长期过期，SOC/PCS两个输入亦再次超过10秒时效；历史控制失败锁仍在，真实PCS闭环未验收。通知页只证明已有历史投递记录及配置入口可读，未发送新通知。详见[现网完成度评估](reviews/2026-09-06-v0.9.5-live-readiness.md)。

本轮按用户要求不调用插件；使用终端、现有发布脚本及无头浏览器脚本。只重建后端，不启动Caddy/TLS，不清理数据，不修改数据库/MQTT与原网络挂载配置，不启用策略、清除失败锁或下发真实控制。
