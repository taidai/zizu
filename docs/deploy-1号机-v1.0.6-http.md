# v1.0.6 发布记录

2026-09-08：1号机已部署，站点保持 HTTP，不启动 Caddy 或申请证书。

## 交付内容

- 同一 Neuron 来源导入多个节点时，分别向各已启用 L0 投递样本，避免单值映射覆盖导致其他实体长期超时。身份、原始值、类型、质量与迟到排序均保留，未放宽新鲜度阈值。
- 调度页面按 Demo 主布局调整：三步横向摘要、全宽原生决策表、输入/输出暂存弹窗、按需预览/JSON/表达式说明。取消不改变草稿，保存/试算/发布和控制安全约束不变。

## 固定制品

- 源码及 tag `v1.0.6`：`340f57911516c89518485aae33977be06ad27d41`。
- GitHub Actions：`34215486703`，success。
- ARM64：`ghcr.io/taidai/zizu@sha256:4bf5da249e991fe5ca7025252d102189b18b31d997757bad13293de458ae9771`。
- 加载 image ID：`sha256:e64f2c5bbe54f86f79f78df6d7d4c5bba8b4c4fe4314cb16590e96ae9534af77`。

## 验证与现场边界

- Backend 774：493通过、281条件跳过；前端模型201/201；发布脚本56/56；production构建8211模块通过。
- 调度浏览器首轮25/26；唯一失败是新测试将认证票据计为业务写，修正范围后该项1/1通过，并非一次全26通过。
- 现场主干9项已通过。调度首次懒加载超过默认5秒检查，保留原失败；仅补验调度与实体新鲜度，通过。6个现有L2均GOOD且来源时间前进；页面/API错误与浏览器业务写入均0。
- 只替换一次backend，healthy/restart0；Schema063不变、配置修订780不变；DB/NanoMQ/Neuron未重启，策略未启用，无控制在途。不宣称已验证真实控制。
- 配置备份：`/opt/zizu-release-test-0.5.0/backups/v1.0.6-pre-20260908T103941Z`；旧1.0.5镜像保留。此次同Schema更新未执行迁移或全库恢复，未把配置备份宣称为新全库备份。
- 现场重型JDM首次加载仍需时间；首页未配置槽位仍需按真实业务含义绑定，不以假值填充。本次不宣称整个EMS已全部交付。

私有验收证据在工作区 `.release-artifacts/v1.0.6/`，含原失败、补验、实体时间对比和真实截图；不提交认证信息及现场数据。
