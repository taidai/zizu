# v0.9.7：数据传递与回读修复发布记录

日期：2026-09-06。发布范围仅为原后端服务；不更换架构、不增加依赖、不改 Schema、不开启 TLS。

## 制品与修复

- 源码/tag：`3e1ce775e767ecf28ad7483d31160c7d109e11a6` / `v0.9.7`；Schema `062`。
- [GitHub 构建 34023104171](https://github.com/taidai/zizu/actions/runs/34023104171) 成功。
- ARM64 镜像：`ghcr.io/taidai/zizu@sha256:6c6836cd9909d93a8ffd876fd3bea5eed5ea3087a7a166cffbcb5ab123f3b424`。
- 首条有效但不匹配的回读不再提前失败；原命令期限内继续只读等待。首条不匹配证据只持久化一次，重启保留；到期有该证据判 mismatch，否则 timeout。新鲜度、质量和原期限保持不变。
- 一个调度意图只下发一次，第一次终态失败就停用并锁定策略、取消待发意图、释放所有权。清除失败不自动启用；旧版本遗留重试不能再次下发。
- 去掉帧路径未使用的查询，整帧批量解析并保存来源；保留事务 A/B、来源语义和提交后可见。

## 发布前证据

- 本轮完整后端：692 项，419 通过、273 条件跳过、0 失败；脚本 56 项、前端 106 项通过，生产构建成功。仅有既存的大 chunk 提示。
- 前一开发轮的真实 PostgreSQL 回读 5 项、调度门禁/恢复 28 项、数据相关组合 92 项通过，独立审查无 Critical/Important 问题。本轮产品源码未再修改，复用该专项证据；这些组合有重叠，不能相加冒充唯一用例总数。
- 本机小样本基准中，5 个输出/10 条来源边由 20 条 SQL 降为 3 条；不以该结果代替现场时效验证。
- 新备份包含 52 张配置表与部署文件，配置归档 SHA256：`13804be864c6a905b03b23b4c3a1c722090a3bb8e13db0cbed1a00ef02e9237b`。
- 在新建隔离数据库中恢复并验证 60 张当前关键表、102 项外键；已删除的旧规则模板仅保留归档、不重建。关键快照 SHA256：`cb6a103b427b928e3393daa198c53572f567be08d411057010be155f5ca1afb8`。这不是全部遥测历史的灾备演练。

## 现场结果

- 17:08:50（北京时间）后端切换成功，`healthy`、restart 0、运行版本 `0.9.7`；四个修改模块与发布源码一致，Schema 仍为 062，启动后日志检查无 ERROR/CRITICAL。
- 原 host 网络、`/dev/mqueue`、挂载、安全选项及运行环境保持不变；数据库和 NanoMQ 的容器身份、启动时间不变。未回滚、未清理数据；镜像下载发生一次 `unexpected EOF` 后由 Docker 正常重试完成。
- 控制安全核对：启用策略、所有权、在途意图和活动命令均为 0；历史命令仍为 3。没有新建或启用策略、没有设备写入。
- 真实 PCS 无头浏览器只读主干 8 步通过，38.515 秒：版本/登录、节点树、L0 实时与刷新、L0 历史、L1 当前加工/模板、L2 实时/历史/来源、告警规则入口和调度策略入口。L0/L2 历史分别读到 986/984 条；无页面错误或越界请求。未试算、发布配置、发送通知或执行控制。
- 首轮自动选择到旧 E2E 对象，其最后数据为 9 月 4 日，最近一小时历史为空，测试提前失败；该证据完整保留。补充轮显式选取前版验收的真实 PCS 链路并验证来源关联，没有按 GOOD 质量筛样。不能把本次页面通过说成旧对象数据已恢复或全部 CRUD 通过。
- 额外可见 Browser 抽查在获取标签时连续超时，未完成；实际页面验收依据独立无头浏览器的成功报告和截图，不把工具超时隐去。

### 时效与真实控制仍未验收通过

对已选通信阈值 L0 做 4 次只读采样（7.512 秒），源数据龄为 5.028～12.516 秒，4 次均 STALE；只看到同一个已提交帧。观察到源时间至接收 0.313 秒、接收至拍照 0.765 秒、拍照至完成标记 2.598 秒；没有采到新的提交可见边界，不能据此计算准确 commit/fsync 耗时。采样窗无待处理积压或新增失败帧，不代表持续性能达标。

真实 PCS 的一次 L2 页面样本数据龄约 9.8 秒；页面显示正常不能代替控制链各自的新鲜度检查。局部批量优化已发布，但尚未证明现场控制数据时效达标；本轮没有重跑真实设备闭环，也不放宽质量或时间门槛。

## 回滚与边界

原 `release.env`、compose 和运行环境文件均已备份。若切换健康检查失败，部署脚本恢复原文件并仅重建后端；回滚镜像为原 v0.9.6：

`ghcr.io/taidai/zizu@sha256:7fce5fb21f2fcc11349742081fb39a1beb821ab9f4756e4aca4c3bd1d401a015`。

无需数据库降级或覆盖现场数据。真实设备写入不属于本轮发布验收，不启用策略、不更改设备参数、不人为触发通知。

完整私有证据位于 `.release-artifacts/v0.9.7/`；不得提交凭据、配置快照、真实拓扑或设备数据。本版发布不等于真实控制闭环或完整 EMS 已交付。
