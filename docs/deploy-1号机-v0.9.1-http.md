# 1号机 v0.9.1：帧发布与刷新修复

2026-09-05 21:47（北京时间）部署完成，应用 healthy、restart 0。本次局部修复已交付；调度整体仍未达到真实控制验收条件。

## 发布内容

- 源码及标签：`v0.9.1` / `d0109a30306c1b9a91add2307b5eccecd0368352`；开发分支已推送，main 未移动。
- [构建 33968861550](https://github.com/taidai/zizu/actions/runs/33968861550)成功，Schema 062 不变。
- ARM64 固定镜像：`ghcr.io/taidai/zizu@sha256:87aaa1273230e1ba011720f2b3e06c73b7fac2efa55954935ef903c5f991cc64`。
- 事务 B 保留完整 L0 基线，第二次只整理本帧更新和新跨 STALE 边界的行，避免重复处理全部陈旧点位；空基线、原值、质量、时间、来源及历史重建语义不变。
- 修复原始点位快照持续失败时，手动刷新按钮无法再次使用的问题。列表/健康请求自身永久悬挂不在本次证明范围内。
- 未改变同步提交、新鲜度期限、采样、控制门槛；未新增缓存、Schema 或依赖。

## 验证

- 后端完整发现 637 项：387 通过、250 条件跳过；另跑隔离 PostgreSQL：新增 outbox 8/8、原数据帧 28/28、调度 47/47。
- scripts 56/56；前端 106/106、生产构建成功，既有 chunk 警告保留。
- 浏览器请求替身回归 9/9；本机真实平台主干 7/7、64.0 秒。首次本机运行因刷新锁定失败，新增回归先失败后修复通过，失败证据保留。
- ARM 同快照只读局部对比：1.505 秒降至 1.003 秒、payload 相同；仅代表该段，不能当作整帧提速或时效达标。
- 现场首轮 `v091_node_214854` 在夹具登录收到代理 502，0 通过、1 失败、6 跳过；没有冒充业务验收通过。随后仅为本次测试进程增加站点 NO_PROXY，未修改系统代理或安全配置。
- 现场第二轮 `v091_node_215519`：7/7、0 失败/跳过、545.998 秒，正常退出并清理本次临时资源。覆盖节点维护、Neuron 导入、L0 实时/历史、L1 检查发布及模板生命周期、L2 质量/来源/单位、BIT 0/1/2 到告警选择、读取失败恢复及退役。超过 5 分钟目标，性能门槛未通过。只永久删除测试备用点及其历史，测试节点/模板退役、临时 Neuron 节点清理；这些删除不可恢复，业务数据和用户新建节点未删除。

## 调度试算与剩余阻断

现场真实界面 `v091_simulate_215446` 完成“保存草稿 → 试算”，HTTP 200 / EVALUATED：同一 committed 帧 309551、配置 760，SOC 99%、PCS 限值 156.8 kW，JDM 命中 other-time，拟议目标 156.7 kW。只是试算，没有生成真实控制命令或写设备。

显式草稿保存把旧基准 732 更新至当时活动配置 760；JDM、绑定、时区、触发设置、已发布版本、停用状态与旧故障锁保持不变。节点测试还会推进配置修订，不能把这次成功永久等同于当前配置就绪。

主干清理结束后的最终 UI 检查 `v091_simulate_220818` 再次 EVALUATED：草稿基准 760→776，帧 309665，SOC 99%、PCS 156.8 kW，拟议 156.7 kW；保持停用和故障锁，未发布。截图已人工检查。完整 health 为 ok / RUNNING，数据库、MQTT、Neuron 连通，解析及写入错误 0；release_lock 仍 unavailable，不能声称发布锁验收完成。成功试算只是两个时点证据，不否定独立年龄采样的失败。

发布后、第二轮现场主干之前的 16.23 秒采样：64 次实体检查中 27 次超过 10 秒，最大 15.54 秒。新鲜度仍不稳定，不能执行真实 PCS 小步闭环。没有通过放宽期限取得通过。页面旧绑定预览可显示“超时”，而新试算帧已新鲜；这项预览刷新体验尚未修复。

## 部署与安全

- 仅替换 backend，容器 `beeffa8d6714`；host 网络、`/dev/mqueue` tmpfs、Compose/runtime 哈希不变，数据库及 MQTT 容器身份/启动时间不变，Neuron 未重启。
- 安全复核：启用策略、控制所有权、活动意图和活动命令均为 0；历史命令仍 3 条；PCS 保持 156.8 kW，原限值与旧故障锁不变。未发送真实通知、清空业务数据、开启 Caddy/TLS 或回退。
- 检查时根盘 3.2 GB、数据盘 3.4 GB 可用。
- 配置备份：`/opt/zizu-release-test-0.5.0/backups/v0.9.1-pre-20260905T131524Z`，52 表、659086 字节，SHA256 `788204faed8676bce856a4496b1c6b6b34d3acd99246b0b80c80237f164c6854`。目录可读；不是全量遥测备份，也未做隔离恢复。
- 回退镜像保留：`ghcr.io/taidai/zizu@sha256:daee5deb1f9b0185682ba3d731ae3f7675e1c005eb5a434dd6a51b4acc95d81a`。仅回退应用，不恢复或清空数据库。

下一步只聚焦剩余帧处理/提交耗时及实际数据年龄，不再扩功能或重复部署。持续时效与现场性能门槛通过前，不启用策略、不清故障锁、不下发真实控制。
