# v1.0.7 发布记录

## 制品

- 发布源码：`2908a6f`，构建分支 `release/v1.0.7`。
- GitHub Actions：`34230224895`，成功。首轮因后端两处版本未同步被门禁拒绝，未部署；修正后重建。
- ARM64：`ghcr.io/taidai/zizu@sha256:941c9d60173b71f0376a812892b12d32daad23c2b99468dd4d159865a6b6852f`。
- Image ID：`sha256:037a7174865ee9ec829018557ea6c9cd81add045a270ce4bab57bc6b74a688ab`。
- Schema 063，无迁移。

## 范围与验证

- 运行总览左侧光伏、电网；电网正值供电、负值返送，储能正值放电、负值充电。零值及非当前数据停止箭头，充电桩方向未约定、保持静态；现场电网槽位须绑定并网功率。本次不改变槽位绑定。
- 前端专项11/11，src测试180/180，脚本56/56；1024/1280无头布局2/2，生产构建通过。后端业务代码未变，未将旧版本测试称为本轮重跑。
- 本次仅更新应用，保持HTTP、host网络和 `/dev/mqueue`，不启用策略、发送通知或控制设备。
- Docker 位于独立 `/userdata` 分区。固定1.5GiB加载门槛首次拒绝；改为实际归档333,516,800字节加512MiB余量后校验通过（加载前空闲1,622,391,808字节）。根分区上传空间另行检查，不删除旧镜像或业务数据。

## 现场状态

- 已部署 `DEPLOYED_HEALTHY`，健康检查版本1.0.7，restart 0。Schema063、配置修订780前后相同，策略启用/所有权/在途意图/在途命令均0。
- 数据库、NanoMQ容器身份及启动时间不变，Neuron进程不变；host网络、`/dev/mqueue`、原挂载和运行配置保持一致。
- 运行配置备份：`/opt/zizu-release-test-0.5.0/backups/v1.0.7-pre-20260908T132758Z`。旧1.0.6镜像保留；本次无数据库迁移，未执行全库备份恢复，不冒充全库恢复证明。
- 传输临时包已验证并删除，可从本机固定制品重新生成；未删除业务数据或旧镜像。
- 现场无头只读主干9项通过，采集消息持续增长，业务写入0、页面/API错误0。覆盖新版首页与左右布局、节点、L0实时刷新/历史、L1打开取消、L2实时/历史/来源、告警、设备与控制目录。首轮L2页面超过默认5秒，保留失败；等待上限20秒复验通过，不将首轮写成通过。
- 可见Browser插件超时，独立抽查仍为INCOMPLETE。未宣称现场所有槽位已正确绑定或真实正负控制已验收；动态方向已由本机数值专项验证。私有证据保存在项目外层 `.release-artifacts/v1.0.7/`。
