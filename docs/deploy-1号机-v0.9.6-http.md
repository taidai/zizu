# v0.9.6：已部署，真实控制验收未通过

日期：2026-09-06。只更新原有后端服务；不更换架构、不新增依赖、不改 Schema、不申请 TLS。

## 版本与范围

- 发布源码/tag：`0a10146c08c07f3964856c2ed8e2bd12ca96839a` / `v0.9.6`；Schema `062`。
- [GitHub 构建 34010849157](https://github.com/taidai/zizu/actions/runs/34010849157) 成功。
- ARM64 固定镜像：`ghcr.io/taidai/zizu@sha256:7fce5fb21f2fcc11349742081fb39a1beb821ab9f4756e4aca4c3bd1d401a015`。
- 12:23（北京时间）后端 healthy、restart 0；五个修改模块与发布源码哈希一致。
- 原 host 网络、`/dev/mqueue`、挂载与安全配置保留；数据库和 NanoMQ 容器身份、启动时间不变。

本版修复：来源有效截止时间贯穿策略门禁、统一控制与实际 Neuron POST；慢查询、鉴权/重鉴权之后仍检查原时效。回读返回或抛错后重新取当前时间，超过原命令期限不得冒充成功。不延长新鲜度或回读超时。

## 验证证据

| 检查 | 实际结果 |
| --- | --- |
| 本轮后端发现 671 项 | 411 通过，260 外部环境条件跳过，0 失败 |
| 发布脚本 / 前端契约 | 56 / 106 通过 |
| 前端生产构建 | 通过；仍有既有大 chunk 提示 |
| 控制补丁专项 | 复用此前 52 项离线、20 个独立真实 PostgreSQL 用例证据；不冒充本轮重跑 |
| 本版五个产品安全补丁独立复核 | 无 Critical / Important；不包含被否决的私有 runner |
| 现网无头只读主干 | 页面检查 34.4 秒；节点、L0 实时/历史、L1 加工/模板、L2 实时/历史/来源、告警入口通过；errors 0、blocked 0 |
| 新目标 L1 配置试算 | 未通过：精确值及 INT/s 类型正确，但 `INPUT_STALE`；未执行 apply |
| 真实 JDM 设备写入 | 未执行，不能计为通过 |

本地汇总工具未带 site-url 时返回 INCOMPLETE；四项本地检查通过，现场 smoke 另行执行。此次 smoke 是只读主干复验，不是全模块 CRUD 或完整 EMS 验收。

## 备份与恢复边界

部署前保存 52 张配置表、部署文件及控制关键记录；配置归档 SHA256：
`21bf779bb3fd5228fd2b6e9d807930bf9c63cdcc8eb471497b14adbe0033fa2d`。
在新建隔离数据库中验证 60 张当前关键表与 102 项外键关系，并将结果归档；隔离环境不运行控制 worker、不连接设备。这不是全量遥测历史灾备演练。

回滚使用该备份中的原 release.env 与原 compose，仅重建后端，恢复 v0.9.5 固定镜像：
`ghcr.io/taidai/zizu@sha256:33c0cb4ddb174d5bde4df25ca409e4b2625ddf52e074ff1e9d8347067633a58d`。
无需修改数据库 Schema 或覆盖数据；本轮没有实际执行回滚。

## 两个尚未解决的阻断

1. **提交延迟**：现场四次只读采样中，原始数据从接收到拍照约 0.42 秒，但读到最新已提交值时已有 3.85～6.82 秒龄。查询仅 15～75 毫秒，固定 3 秒实时保护正确返回 STALE。既有写盘/WAL 等待证据支持继续定位冻结后到提交可见的路径；不能把本次快照直接解释为某一次 fsync 的耗时。不得改用拍照时间、刷新时间戳或放宽门槛换取通过。
2. **单次写保证不足**：原策略失败最多尝试三次；外部观察命令后调用停用，不是服务端原子执行预算。进程或 API 延迟期间仍可能重试。私有实验 runner 未获批准、未执行，不是已交付能力。

仅产生过一个未应用的配置预览；未发布新实体、未新建/启用验收策略、未清理旧失败锁、未写设备或发送通知。私有恢复脚本后来修正了预览结果的落盘顺序，但未再次连接现场，仍需独立审查。

下一步先解决并验证数据提交时效，再明确严格单次验收与既有重试语义的边界。保留原 JDM→统一控制→新 committed L2 回读主干，不以手工直写 Neuron 代替验收。

完整现场证据保存在私有 `.release-artifacts/v0.9.6/` 与 `.release-artifacts/control-closeout-20260906/`；不提交凭据、设备参数或客户拓扑。此发布不代表完整 EMS 或真实调度已经交付。
