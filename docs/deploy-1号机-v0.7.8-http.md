# v0.7.8 告警中心加载修复部署记录

日期：2026-09-03。范围：告警列表查询/加载性能；不改变告警判断、通知或控制语义。

## 发布

- 源码/tag：`34f53b87e6aeff77107ddc94cc8d076e65cc9029` / `v0.7.8`。
- [Actions 33703328330](https://github.com/taidai/zizu/actions/runs/33703328330)：成功，双架构清单校验通过。
- ARM64：`ghcr.io/taidai/zizu@sha256:3db7dd6e0f98e426bf69189e8f81febbe6522974c9f22540f04db1a501c22285`。
- 运行镜像 ID：`sha256:08d14bb5e0957d84dc5bd2f0c717acf314d1d8592be43b8d8ac27a87657072e1`。
- 2026-09-03 09:31–09:32 北京时间，仅重建 `zizu-release-test-backend-1`。
  Schema 060 不变，健康状态 healthy，restart 0，保留 host 网络及 `/dev/mqueue` tmpfs。
  PostgreSQL 与 NanoMQ 的启动时间未变；未启动 Caddy，未申请证书。
- 首页及 `/api/v1/health/live` 均 HTTP 200；后端与前端均为 0.7.8。

## 性能与功能证据

同一台机器回环 HTTP 实际服务测量，不是候选代码或模拟响应：

| 项目 | 修复前 | 部署后 |
| --- | --- | --- |
| 当前告警首页 | 4.536 / 6.845 秒 | 0.094 / 0.308 / 0.156 秒 |
| 已恢复历史首页，50 条 | 未单独记录 | 0.299 秒 |
| 已恢复历史第二页，50 条 | 未单独记录 | 0.794 秒 |

部署后历史总数 6,365，查询返回最多一页。现场数据持续变化，不把事件数变化当作配置变更。
公网 Chromium 无头浏览器最终一轮当前告警请求 0.273 / 0.918 秒，历史首页 1.481 秒，均 HTTP 200；
公网耗时包含传输及其他运行负载，不混用回环数据承诺所有用户固定响应时间。

无头浏览器使用真实页面、真实登录与真实服务，只读操作，没有 HTTP 数据模拟：

- 节点树 → 原始数据：91/91 正常，BIT 原值为数值 0，质量、时间、来源可见。
- 标准实体 → 数据来源与计算：本节点加工已生效，两个输出、来源与配置修订可见。
- L2：两个实时实体分别为布尔 false、数值 0，质量正常，有新时间戳。
- 告警中心：当前事件、已恢复历史及返回未恢复筛选正常。
- 测试阻断配置写请求，仅放行登录和实时订阅所需的 WebSocket 短期凭证申请；未确认告警、
  未改规则、未触发测试通知、未控制设备。临时浏览器已关闭。

内置 Browser 核对了新版首页与版本；主干操作因连接两次超时未完整完成，最终主干证据来自项目
现有 Chromium 无头浏览器。首次无头尝试误拦截订阅凭证，不能算实时数据通过；修正测试边界，
等待实际数据后重新断言，最终通过。没有安装新浏览器或新依赖。

## 回归

- 本次发布前后端 unittest 510 项（191 条件跳过），无失败；发布脚本 56/56。
- 前端 mjs 88/88；TypeScript + Vite 生产构建成功，保留既有大 chunk 警告。
- v0.7.8 生产构建上无头加载专项 2/2，2.4 秒：后台刷新不隐藏列表，旧响应不覆盖新筛选。
- PostgreSQL 临时表专项 2/2 及修复前后根因证据见 `reviews/2026-09-03-alarm-center-loading.md`。
- 站点配置、告警定义、活动定义引用、HTTP 通知配置的整表内容摘要在切换前后一致。

## 备份和回滚

- 保留原 v0.7.7 镜像：`ghcr.io/taidai/zizu@sha256:c32f45d46cc3cebd2f5c7249941d527abea10c0dee7d853baeb64cf57743ad45`。
- 配置副本：`/opt/zizu-release-test-0.5.0/backups/v0.7.8-pre/release.env-20260903T012109Z`。
- 数据库快照开始于切换前，后台完成于切换后：
  `/opt/zizu-release-test-0.5.0/backups/v0.7.8-pre/omnithings-20260903T012109Z.dump`。
  520,216,227 字节，SHA256 `2ea9070644e181c4a142bca0d2c4c13fc2d571514552d41e3e1be02bb4774be8`。
  `pg_restore --file=/dev/null` 全量解码读取成功，目录输出 1,502 行。
  本轮没有做数据库隔离恢复，不能把归档读取校验称为恢复演练。
- 原备份 SSH 读取等待超时，确认远端 pg_dump 仍在运行后未重复启动；结束后独立校验归档。
  本次无数据库结构变更，故镜像切换没有等待全库压缩完成。
- 回滚时先确认上述配置副本及旧镜像仍存在，再在部署目录执行：

```sh
cd /opt/zizu-release-test-0.5.0
cp -p backups/v0.7.8-pre/release.env-20260903T012109Z release.env
docker compose --env-file release.env -f docker-compose.test.e606.yml up -d --no-deps backend
```

这是软件回滚，不恢复数据库、不删除新数据。实际回滚仍需检查现场是否有后续新版本或配置改动。

## 结论和边界

本轮告警加载修复及部署验收 **PASSED**。不等于全平台 CRUD、通知失败重试、JDM 或控制链全面验收。
HTTP 通知的既有缺口不在此次范围。未删除历史数据、备份或旧镜像。
根分区剩余约 1.6 GB，使用率 90%；后续应安排磁盘维护，但不能直接清除唯一恢复锚点。
