# v0.9.9：固定节拍 JDM 纵向验收底座

日期：2026-09-06。仅替换 1 号机原后端服务，保持 HTTP、host 网络、`/dev/mqueue`、数据库、MQTT 和运行环境不变。

## 制品与范围

- 发布源码：`6c66dd2855992896e5f7d116d7b865f42e491119`；标签 `v0.9.9`，Schema `062`。
- [GitHub 构建 34041981922](https://github.com/taidai/zizu/actions/runs/34041981922) 成功，构建来源与发布提交一致。
- ARM64 固定镜像：`ghcr.io/taidai/zizu@sha256:c9b17b8cbfd3f40338364105fb092cbe5ee3c086f321f3dfe7067dff9a623559`。
- 本版补齐固定节拍 JDM 的本机真实后端无头纵向验收：浏览器、FastAPI、PostgreSQL/TimescaleDB、标准 GoRules JDM、固定节拍、数据帧 outbox、统一控制和 committed L2 回读均使用真实实现，只替代设备协议边界。
- 成功链证明单次下发、同一分钟不重复、回读确认；失败链证明命令失败后策略停用且不自动重发；另一路证明告警与策略绑定同一 committed L2 身份。
- 未改变产品控制语义、质量门或数据库 Schema。`FLOAT` 点收到 JSON 整数字面量会形成 `TYPE_MISMATCH`，仍作为已知产品行为保留。

## 发布前证据

- 完整门禁：后端 699 项（425 通过、274 条件跳过）、脚本 56 项、前端 106 项全部通过；前端生产构建成功，仅有既有大包警告。
- 调度专项最终为 13/13 无头用例通过；fixture 与启动架构守卫 23/23 通过。独立复审最终为 0 Critical、0 Important、0 Minor。
- 现场配置备份 52 张表，SHA256 `2ae76287069efc3e137635d0b7954af3c04c0daf604d09109d8ae98be40ffa3e`，目录 `/opt/zizu-release-test-0.5.0/backups/v0.9.9-pre-20260906T153022Z`。
- 新建隔离库恢复并验证 60 张当前表、102 项外键；关键快照 SHA256 `eeb81e52f494a9e0d8d92ba213ab95c2998c8f612d4a3c7f5fd2fe6874dc4721`。旧规则模板只归档，不重建；不包含 L0/L2 遥测历史。
- 部署前启用策略、控制所有权、在途意图和活动命令均为 0；历史命令为 3。

## 现场部署与验收

- 23:51（北京时间）切换完成：后端 `healthy`、restart 0、版本 `0.9.9`；Schema062，核对模块源码 hash 与本机一致，新启动日志 26 行中无 ERROR/CRITICAL。
- 数据库和 NanoMQ 的容器 ID、启动时间及 restart 计数不变；compose、runtime、host 网络和 `/dev/mqueue` 均保持不变，未回滚、未清理数据。
- 切换后启用策略、控制所有权、在途意图和活动命令仍均为 0，历史命令仍为 3；没有设备写入、策略试算/发布/启用或通知发送。
- 无头浏览器只读主干 8 步通过，43.032 秒，无 findings、errors 或 blocked：覆盖版本/登录、节点树、L0 实时刷新与历史、L1 当前加工与模板入口、L2 实时/历史/来源、告警入口和 JDM 只读入口。读取 L0 历史 982 条、L2 历史 981 条。
- 选定真实 PCS 点位当次 L0 为 `156.8 / GOOD`，对应 L2 同值同单位且来源证据完整；但 L2 数据龄 `12.495s`、`fresh=false`。因此页面与数据主干只读验收通过，**真实设备控制闭环仍为 INCOMPLETE**，继续保持策略停用。

## 回滚与下一步

回滚镜像为上一版固定摘要：

`ghcr.io/taidai/zizu@sha256:3f46da666952d30ea9080f170f6bc3d379f6a4aec21de735d43021decf4fb365`。

最短下一步不是扩功能，而是完成 Task 9：先让选定安全写点对应 committed L2 连续满足既有新鲜度与质量门，再在现场明确授权下执行一次小幅“改值 → 新 L2 回读确认 → 恢复原值 → 再次确认”，最后停用策略并核对无所有权、无在途意图、无活动命令。未完成该证据前，不开展真实 2充2放功率调度。

完整私有证据位于 `.release-artifacts/v0.9.9/`，不得提交凭据、配置快照、现场拓扑或设备数据。
