# HTTP 未通知排查与请求体光标修复

## 状态

2026-09-03，基线 `d2588a3`，现场 v0.7.9 / Schema060。本轮为未提交的本地修复，未部署。
现场旧规则处置待用户确认，因此现场问题关闭状态为 **INCOMPLETE**。

## 现场只读结论

- 同一实体存在两条独立规则：旧规则触发和恢复均为 false，未绑定通知；新规则 true 触发、false 恢复，已绑定已启用且测试有效的渠道。
- 读取时 L0 原值为 0、L2 为 false、质量正常。旧规则不断触发/恢复，新规则未触发，不能把二者混为一条。
- 历史通知已有 HTTP 200，接收端业务响应 code=0 / StatusCode=0。证据支持该次请求被接收，不证明用户已阅读。
- 本轮未修改现场规则、启停渠道、重试通知或操作设备。通知绑定属于规则，不属于实体；不能给另一条旧规则偷偷继承绑定。

## 最小代码修复

- 请求体变量按钮从无条件追加，改为按 textarea 原生 selectionStart/selectionEnd 插入或替换；React 提交后恢复焦点和光标。
- 中文、多行、emoji、连续插入与继续键入均保留原有后缀。变量仅用于请求体；没有扩展 URL、参数或请求头模板语义。
- 触发与恢复条件完全相同，试算/生成计划会明确阻断；未应用的旧 ready 计划在发布前重新校验，已应用计划仍可幂等读取。
- 中文错误映射与 README 已更新。旧活动事件/规则不自动修改，历史消息不自动补发。

## 验证证据

- 光标红灯：真实 React + 无头 Chromium 三个预期断言失败；修复后专项 8/8。
- 条件冲突红灯：false/false、true/true 未被拒绝；修复后试算、计划、发布均阻断，相反条件继续可用。
- 审查发现旧 ready 计划可绕过，独立复现后补测试及发布校验；二次只读复核关闭该 P2。
- 后端完整 unittest：516 项，退出 0，191 项 PostgreSQL 条件测试 skipped，21.1 秒。
- scripts：56/56；前端全部 mjs：88/88；npm run build：退出 0，既有大包警告。
- 新生产构建在本机 preview 上：光标 8 + 通知选项 4 + 告警加载 2，共 14/14，8.9 秒。测试模拟外部 API，不发送现场通知。
- 现场无头只读走通节点树 → L0 → L1 → L2 → 告警及通知选项；91 L0 正常，2 个 L2 可读。该证据是旧版运行主干，不能冒充新修复已上线。
- git diff --check 通过。测试运行目录 `frontend/test-results/http-cursor-production-final/`。

## 下一步

确认是否停用未绑通知且触发/恢复相同的旧规则，保留正确且已绑渠道的规则；不要替用户推断故障极性。
部署修复后重新核对线上变量插入。正式通知闭环需要获准的隔离事件或真实故障/恢复，不能改设备值制造故障。
本轮未升版、提交、推送；后续发布按既定版本进位规则执行，并保留用户原有未提交资料。
