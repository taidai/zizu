# 真实控制闭环：写前时效与回读期限修复

日期：2026-09-06。基线 v0.9.5 / `894c1e4` / Schema062。

## 结论与边界

本轮修复真实写入前必须守住的两项时间边界，代码在本地工作树，尚未发布。
真实 PCS 小幅设定与恢复尚未执行，闭环验收结论为 **INCOMPLETE**。
不把隔离测试、HTTP200、历史记录或一次试算成功当作设备动作证明。

用户要求仅采用 GPT 官方最佳实践。本轮依据
[OpenAI 模型指南：按风险选择验证范围](https://developers.openai.com/api/docs/guides/latest-model)
与[Codex 最佳实践：测试和复核](https://learn.chatgpt.com/guides/best-practices)，
使用原有代码、终端和无头脚本；不启用其他插件工作流，不变更模型或全局插件设置。

## 已修复

1. 回读查询开始时未超时，结束时已经超时，旧实现仍可能确认成功并把事件记在查询之前。
   现在读取返回或异常后重新取时间，达到原截止时间即记超时，不延长原超时期限。
2. 策略写前检查使用数据库查询之前的时间，慢查询、审计、冷却登记或 Neuron 登录可能耗尽输入有效期。
   现在读取完成后重新验证持久意图和当前 committed L2，按每个绑定的观测时间与新鲜度求最早截止。
   该截止只在进程内沿现有统一控制链传递，不接受前端或证据自报，不新增数据库字段。
3. 审计和冷却处理后再次检查；Neuron 初次登录、401/403 重新认证结束后，在发送真实写请求前再检查。
   过期明确拒绝，策略缺少合法截止时间同样拒绝，不以缺值回退绕过。
4. 同实体有多个不同新鲜度的绑定时取最严格截止；无关额外样本不放宽截止或导致异常。

保留原有来源/配置栅栏、类型单位、限值、联锁、幂等、重试、故障锁及第一条新 GOOD 不符回读的 mismatch 语义。
不增加运行模式、调度器、控制旁路或依赖。原始点位与设备寄存器配置未修改。

## 验证

- 控制、Neuron、调度及新增截止边界离线专项：52 项通过，新增关键失败场景先红后绿。
- 后端全量发现669项：411通过、258条件跳过、0失败，31.169秒；跳过项不计通过。
- 隔离 PostgreSQL 原有意图门禁18项通过；随后新增慢读取消、真实观测截止和受影响的实际控制入口共3项通过，合计20个不同数据库用例。只使用已有专用测试库。
- 慢数据库导致旧时间放行、慢回读导致超时后误确认均有确定性重现；这些软件缺陷不是历史 PCS 不动作的已证实唯一根因。
- 无前端产品改动，不重复执行前端构建或扩大性能测试。
- 独立只读复核：无新增 Critical/Important；主代理发现的多绑定截止问题已修复并补回归。
- 现网 v0.9.5 原基线无头主干六视角通过，导航约17秒、含健康读取约19.4秒，无页面异常或越界业务写。截图已核对；这不是尚未部署补丁的现网验收。

## 现场只读事实

- 现有真实目标仍是最大放电功率限值，唯一源路径、类型、小数倍率、安全范围和回读政策与既有记录一致。
- 两个策略保持停用，没有所有权或待发意图；历史仍是三次失败命令，没有清锁或新增控制。
- 本轮短时读取仍见控制输入超过既有10秒新鲜度门槛，因此不能据此批准真实写入。
- 在设备通信链路上进行有界被动观察，未安装抓包依赖、未增加设备读写请求。
  双向观察确认目标寄存器三次读取值与当前工程值换算一致；短窗未观察到写请求。
  该主机可见流量不足以排除其他网络位置或其他时间的控制器覆盖，也不能还原过去三次失败的设备响应。
- 最初单向观察未匹配到目标，后续修正观察范围取得请求/响应对应证据；不把前两次空结果当作设备没有上报。

私有证据位于 `.release-artifacts/control-closeout-20260906/`。设备地址、实体身份、现场参数和原始通信摘要不进入公开仓库。

## 后续验收顺序

1. 发布已验证安全补丁；不以热替换代码或清除旧失败锁代替版本发布。
2. 查清阻断实际写入的输入时效，保持现有安全门槛；执行时重新确认现场许可、唯一写点、当前值、限值和联锁。
3. 仅执行已确认的小幅设定，联合记录策略修订/输入帧、控制命令、Neuron响应、设备协议响应与新 committed L2 回读。
4. 新L2确认后才执行恢复；任一步不满足门槛或失败立即停止，不修改其他寄存器、不扩大变化量。
5. 只有设定与恢复均有新回读确认，才标记该真实闭环通过；完整充放电调度另行验收。
