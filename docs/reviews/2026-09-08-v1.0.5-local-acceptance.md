# ZiZu v1.0.5 Task 9l 最终本机验收

日期：2026-09-08（Asia/Shanghai）

冻结可执行 HEAD：`938c018758d4810e673e5b88e38d8639f172c344`

结论：**PASSED_WITH_ACCEPTED_EVIDENCE_DIFFERENCES**

## 结论边界

当前 production bundle、固定 route fixture、真实 PostgreSQL 纵向门禁、禁入扫描和双视口视觉证据支持进入发布元数据/CI 阶段。该结论不是现场验收或部署通过；本轮未访问 1 号机、未 push、升版、tag、部署、发通知、发布规则或下真实控制。

保留两项已接受差异：

1. **组合式 harness 证据。** 当前 HEAD 的 local harness guard 仍只接受旧候选，本任务没有修改 harness，也不把旧运行冒充 current fresh。按维护者 ledger ruling，采用已独立复审的 `506f360` run `formal_143223b2cd814b299302223fe8ccb67b`（real node 7/7、alarm HTTP 3/3、0 skip/retry/flaky、main/independent cleanup exit 0、secret scan 0），与当前 production preview 121/121、dispatch 3/3 组合。`506f360..a44a169` 的精确评审范围仅为 docs、E2E、AdminPanel/FaultMap/AlarmHttp focus 及 hook，无 backend API/migration；`3fce504..938c018` 只删除误跟踪的内部 SDD 报告，且 frontend/backend/scripts Git tree 分别严格相等。
2. **故障映射焦点债务。** final whole-branch fix wave 中，同一未改用例 `tablet-tools.spec.ts:455` 曾一次出现保存刷新后重新渲染的编辑按钮未获焦点；前一轮和本次 121 条单次门禁都通过。原因未建立，不靠重跑、加时、skip 或弱化断言掩盖，登记为 post-v1.0.5 accepted UI debt。

## 冻结、谱系与复用边界

- 验收起始 frozen HEAD 为 `938c018758d4810e673e5b88e38d8639f172c344`，merge base 为 `2f36774903a5a1d19db015c292f3505798a3d1c7`；起始 tracked status 空，diff-check 通过。
- `git diff --name-status 3fce504..938c018` 恰好一项：删除 `.superpowers/sdd/2026-09-08-demo-final-product-parity-implementation/task-final-review-fix-wave-report.md`（133 行）。
- 两端 Git tree 一致：frontend `a6af849a24b44ce7ca924a48e108ff9192f96e9b`、backend `e6b46794ac72ac9715a2ca6bc21c039579e7c825`、scripts `df67e9d6978295d1afd393dc67000fbc75d5a56e`。因此本轮已完成的 frontend/scripts/backend/build/focused 证据可跨该仅文档删除提交复用，不重跑已绿大门禁。
- 固定 route fixture 仍是 brief 的 13 个文件。Task 9m 在既有 focus 文件内新增并复审 3 条覆盖，使 106 变 109；final-review fix wave 又在同一固定文件集内新增 12 条，合法计数为 121。文件列表未扩张。

## 门禁总表

| 门禁 | 结果 | exit / 时长 | 证据 |
| --- | --- | --- | --- |
| frozen clean + merge-base/diff check | PASS；起始 status 空；谱系和三棵产品树如上 | 0 | 本报告“冻结、谱系” |
| frontend model + support | **201/201**，0 fail/skip | 0 | `frontend/test-results/final-fix-wave-model-support.log` |
| scripts discovery | **56/56**，0 fail/skip | 0 | `.release-artifacts/v1.0.5/local-acceptance/a44a169d66e9/logs/scripts-unittest.log` |
| backend discovery + real Zen | **773 total = 492 pass + 281 conditional skip**，0 failure/error | 0 / 141.454s | `frontend/test-results/final-fix-wave-backend-final.log` |
| slot service/API focused | **29/29**，真实 gate + blackboard | 0 | `frontend/test-results/final-fix-wave-backend-focused.log` |
| Schema 063 workbench PG | **9/9**，0 skip；absent-before/after true | setup/test/cleanup 均 0 / 17.325s | `.release-artifacts/v1.0.5/local-acceptance/a44a169d66e9/logs/workbench-postgres.log` |
| production build | PASS，**8,210 modules**；仅既有 chunk warning | 0 | `frontend/test-results/final-fix-wave-build.log` |
| 4195 readiness | `/`、主 JS、主 CSS 均 200，MIME 正确，listener 属于本 worktree preview | 0 / 1 次条件轮询 | `.release-artifacts/v1.0.5/local-acceptance/938c018758d4/logs/preview-readiness.log` |
| 固定 13 文件 production route fixture | **121/121**，0 fail/skip/retry；只运行一次 | 0 / 369.134s | `.../938c018758d4/logs/fixture-121.log`；`frontend/test-results/task9-final-fixture-938c018758d4/` |
| dispatch / JDM / control PG | **3/3**，0 fail/skip/retry | 0 / 约 2.6m | `.../a44a169d66e9/logs/dispatch-postgres.log` |
| real node + alarm HTTP | **7/7 + 3/3**，0 skip/retry/flaky；cleanup/secret clean | 0 / 59.01s + 28.49s | `local-harness/formal_143223b2cd814b299302223fe8ccb67b/result.json`；组合证据差异见上 |
| corrected prohibited scan | source 0、production dist 0 | 0 / 0.155s | `.../938c018758d4/logs/prohibited-content-scan.log` |
| production 视觉矩阵 | **18/18** 新图，capture 1/1；逐图人工复核通过 | 0 / 40.1s | `.../938c018758d4/manifest.json` 和同目录 PNG |
| 最终清理 | 临时 listener/随机 DB/MQTT/owned 进程均 0；仅批准 55439 保留 | 0 | 本报告“资源清理” |

281 条 backend conditional skip 没有被算作通过；真实 PostgreSQL 与纵向覆盖由 Schema 9/9、dispatch 3/3、focused 29/29 和明示为组合证据的 node/alarm 7/7+3/3 补足。

## 当前 HEAD production-preview 单次门禁

readiness 在测试前条件轮询一次即通过：`/` 为 200 `text/html`，`/assets/index-rrLWCMZ3.js` 为 200 `text/javascript`，`/assets/index-1H5OLsD7.css` 为 200 `text/css`；listener PID 50424 的命令属于当前 worktree `vite preview --strictPort`。

```powershell
npm.cmd run preview -- --host 127.0.0.1 --port 4195 --strictPort
$env:ZIZU_E2E_BASE_URL = 'http://127.0.0.1:4195'
node node_modules/@playwright/test/cli.js test `
  e2e/tablet-shell.spec.ts e2e/tablet-runtime.spec.ts e2e/tablet-devices.spec.ts `
  e2e/tablet-control.spec.ts e2e/tablet-processing-recovery.spec.ts `
  e2e/tablet-applications.spec.ts e2e/tablet-tools.spec.ts e2e/alarm-loading.spec.ts `
  e2e/alarm-rule-toggle.spec.ts e2e/alarm-http-options.spec.ts `
  e2e/alarm-http-template-cursor.spec.ts e2e/raw-point-refresh.spec.ts `
  e2e/dispatch-strategy.spec.ts --grep-invert '调度策略本机真实纵向验收' `
  --project=chromium --workers=1 --retries=0 `
  --output='test-results/task9-final-fixture-938c018758d4' --reporter=line
```

执行精确收集 121 条并得到 `121 passed (6.1m)`；没有第二次 fixture 运行，也没有通过重跑挑绿。故障映射焦点用例本次通过，但仍按上节保留 accepted debt。

## corrected scan

source 扫描范围为 `frontend/src`、`vite.config.ts`、`index.html`、`package.json`，排除 `*.test.mjs`；匹配 Demo 语义词与已冻结固定演示数值。bundle 扫描只对 production `dist` 使用语义模式，保留既定 vendor/fixture 排除边界。两次 `rg` 均以 exit 1 表示无匹配，门禁聚合 exit 0：source hits 0、bundle hits 0。

## 18 张 production bundle 图

当前 HEAD 新拍 7 页 × 2 视口，加 HTTP/fault-map 长编辑器 × 2 视口，共 18 张；manifest HEAD 与 frozen HEAD 完全一致，retries=0。逐文件 SHA-256 与实际 PNG 复算一致，尺寸为 9 张 1024×768 和 9 张 1280×800。

人工逐图检查结论：公共品牌和导航层级一致；workbench 的当前值/最后值/需选择/未配置真实状态均可辨；monitor 六卡分页与异常/未配置状态不伪造；control 安全门入口明确；tree 的物理树与 L0/L1/L2 身份清楚；alarm、JDM、admin 的密集表格/卡片没有整页横向溢出；HTTP 与故障映射父子 modal 栈、关闭路径和活动焦点可辨。所有图 `horizontalOverflow=false`，所有 pageerror/requestfailed 为 0，主要可操作触点均 ≥44px 且无 viewport 内遮挡。tree 的受控 503 错态与 strategies 的受控 500 错态会各产生预期 console resource error，页面仍显示真实失败状态，未被当作空态或成功。

## 资源清理

- owned preview 已通过会话 Ctrl-C 停止；4195/4174/19026/19027/19091/21994/21995/17027 均无 listener。
- PostgreSQL 查询确认匹配 `^zizu_(task8|task9|v105_workbench|v105_node|v105_alarm)_[0-9a-f]{32}_test$` 的随机库为 0。
- `zizu-v105-task9-mqtt-*` 临时容器为 0；旧 reviewed harness 的 independent proof 同样记录数据库、broker、进程、Job 与 secret leak 全空。
- 只保留获批 `zizu-v105-task9-db` 的 127.0.0.1:55439 listener，未停止或删除该容器，也未碰其他容器。

## 后续事项

- post-v1.0.5 单独诊断并修复故障映射保存后的焦点恢复，不把本次通过解释为债务已消失。
- local harness 若要用于后续候选，应在独立、受复审的 harness-only 变更中替换旧候选 guard；本报告继续把当前证据称为组合证据。
- Task 10/发布流程仍须自行完成版本、CI、部署和现场验收；本报告只关闭最终本机产品验收。
