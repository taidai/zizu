# ZiZu v1.0.5 Task 9g 最终本机验收

日期：2026-09-08（Asia/Shanghai）
候选 HEAD：`170f7c526412da6ddc45a112bb98f636463dff99`
结论：**INCOMPLETE —— ENVIRONMENT_BLOCKED / TEST_CONTRACT_MISMATCH**

## 阻断结论

按权威 Task 9g brief 使用同 HEAD production `dist` 和 `vite preview --strictPort` 运行 106 条 route fixture。单次完整执行收集 106 条，实际 **96 passed / 10 failed / 0 skipped / 0 retry**，exit 1，216.810 秒。

10 个失败全部来自 `frontend/e2e/tablet-devices.spec.ts`。其 `mountDeviceMonitor` 在 168、175–177 行直接导入 `/@react-refresh`、`/@id/react`、`/@id/react-dom/client` 和 `/src/pages/DeviceMonitorPage.tsx`，这些是 Vite dev 服务器的源码模块路径，不是 production preview 入口。第 61 条起每个设备 fixture 均在第一个导入失败：

```text
TypeError: Failed to fetch dynamically imported module:
http://127.0.0.1:4195/@react-refresh
```

这是验收测试契约与 brief 的 production-preview 硬要求冲突，不是产品 build 失败。不允许退回 `vite dev`、放宽断言或重用旧图来将它写成通过。根据 stop condition，后续 dispatch 3/3、禁入扫描、18 图及视觉复核未执行，Task 10 不得开始。

## 冻结与谱系

- 已先将三处行尾空格和一个 EOF 空行从 accepted spec 机械删除，未改字词；单独提交 `170f7c5 docs(spec): clean final parity whitespace`。
- `git diff --name-only 506f360..170f7c5` 恰好只有 `docs/superpowers/specs/2026-09-08-demo-final-product-parity-design.md`。
- Git tree 等价：`frontend ec0c8c1`、`frontend/e2e 94a49b7`、`frontend/src b6b3426`、`backend b3ecbeb`、`backend/tests 8d37253`、`scripts df67e9d`，在 `506f360` 和 frozen HEAD 上分别相等。
- accepted harness 的 16 个顶层 `.py/.conf` 文件逐文件 SHA-256 全等，mismatch=[]。
- 本次 build 后 production `dist` 仍为 111 个文件，总摘要 `e38f7863732bb31fa26000cc4707aeffcb7d259c113e8c267555bb68abefc4c3`，与 accepted Task 9e manifest 逐文件及总摘要均相等。
- 谱系日志：`.release-artifacts/v1.0.5/local-acceptance/170f7c526412/logs/lineage-digest-proof.log`。

因此复用经独立复审的 Task 9e accepted run `formal_143223b2cd814b299302223fe8ccb67b`：

- node **7/7**，alarm HTTP **3/3**；
- 0 failed / skipped / retry / flaky；
- main exit 0，independent cleanup exit 0，secret scan 0；
- `owned_remaining=[]`、`concerns=[]`；
- accepted manifest SHA-256 `afba0cc6c8381c3ad965b68c560c8856e10790f8234d0e64a16865d39b456c8f`。

## 实际门禁

| 门禁 | 结果 | 退出 / 时长 | 证据 |
| --- | --- | --- | --- |
| tracked status + merge-base diff-check | PASS；status 空，diff-check 无输出 | 0 | 终端记录；merge-base `2f36774903a5a1d19db015c292f3505798a3d1c7` |
| Zen import | PASS；Python 3.13.2 加载临时 `zen` package | 0 | backend discovery 日志开头亦显示 GoRules loaded |
| 前端 model + support | **201/201**，0 fail/skip；口径 179 product-model + 22 support | 0 / 0.752s | `logs/frontend-model-support.log` |
| scripts unittest discovery | **56/56**，0 fail/skip | 0 / 5.879s | `logs/scripts-unittest.log` |
| backend unittest discovery | **771 total = 490 pass + 281 conditional skip**，0 fail/error | 0 / 48.675s | `logs/backend-discovery.log` |
| Schema 063 workbench PG | **9/9**，0 skip/error | 0 / 21.299s | `logs/workbench-postgres.log` |
| production build | PASS，8,209 modules；仅既有 chunk-size warning | 0 / 73.250s | `logs/frontend-build.log` |
| production route fixture | **96/106 passed; 10 failed**，0 skip/retry | **1 / 216.810s** | `logs/fixture-106.log`；`frontend/test-results/task9-final-fixture-170f7c526412/` |
| dispatch / JDM / control PG | NOT RUN | — | fixture 硬失败后按 stop condition 停止 |
| corrected prohibited-content scan | NOT RUN | — | 同上 |
| 14 主页 + 4 长编辑器图 | **0/18，NOT RUN** | — | 同上；不复用 Demo golden/旧 lane 图 |

281 条 backend conditional skip 不计为通过；需真实 PG/纵向覆盖的部分由已运行的 Schema 063 和经摘要等价复用的 node/alarm 门禁补足，但最终 dispatch 未因此免除。

## 准确命令

```powershell
$jsTests = @(rg --files src e2e/support -g '*.test.mjs' | Sort-Object)
node --experimental-strip-types --test --test-isolation=none --test-reporter=spec @jsTests

C:\veighna_studio\python.exe -B -m unittest discover -s scripts -p 'test_*.py' -v

$env:PYTHONPATH = "$zenPackages;$backend;$root"
C:\veighna_studio\python.exe -B -m unittest discover -s tests -p 'test_*.py' -v

C:\veighna_studio\python.exe -B -m tests.run_postgres_group tests.test_ems_workbench_slots_postgres

npm.cmd run build
npm.cmd run preview -- --host 127.0.0.1 --port 4195 --strictPort

node node_modules/@playwright/test/cli.js test `
  e2e/tablet-shell.spec.ts e2e/tablet-runtime.spec.ts e2e/tablet-devices.spec.ts `
  e2e/tablet-control.spec.ts e2e/tablet-processing-recovery.spec.ts `
  e2e/tablet-applications.spec.ts e2e/tablet-tools.spec.ts e2e/alarm-loading.spec.ts `
  e2e/alarm-rule-toggle.spec.ts e2e/alarm-http-options.spec.ts `
  e2e/alarm-http-template-cursor.spec.ts e2e/raw-point-refresh.spec.ts `
  e2e/dispatch-strategy.spec.ts --grep-invert '调度策略本机真实纵向验收' `
  --project=chromium --workers=1 --retries=0 `
  --output='test-results/task9-final-fixture-170f7c526412' --reporter=line
```

route fixture 执行时明确设置 `ZIZU_E2E_BASE_URL=http://127.0.0.1:4195`，且已证明 listener 命令为本 worktree 的 `vite preview`、HTTP 200，不是残留 dev server。

## PostgreSQL 与清理

- 随机库：`zizu_v105_workbench_724d5de7bc0a4c48bdcace9218921ecb_test`；正则保护通过，`ABSENT_BEFORE=True`。
- 凭据从获批容器环境读入内存，未输出/写入报告。容器未显式设置 `POSTGRES_USER`，因此使用官方镜像默认用户 `postgres`。
- 首个预备包装器在数据库生成/创建前因 WSL stdout NUL 编码解析停止，分类为 `TOOLING_PRECHECK_ENCODING`；无 DB/进程副作用，没有运行 PG 测试。修正内存解析后实际门禁执行一次并通过。
- `finally` 只终止该随机库连接并精确 `DROP DATABASE`，然后 `ABSENT_AFTER=True`。
- preview PID 19052 的命令行与 worktree/4195 归属匹配；测试结束后通过会话 Ctrl-C 停止。最终 4174/4195/17027/19026/19027/19091/21994/21995 均无 listener。
- 获批的 `zizu-v105-task9-db` 与 55439 依 brief 保留，未停止容器。
- tracked status 最终在写报告前仅会包含本轮有意的文档改动；无产品、测试、版本或部署改动。

## 未解决项与下一步

1. 在单独、可复审的 test-only 变更中，将 `tablet-devices.spec.ts` 的挂载方式改为消费 production bundle 的公开页面/路由，或为 production preview 提供受测试保护的等价挂载 seam；不得退回 dev server。
2. 该变更将产生新 HEAD；复审清洁后，至少重启受影响的 final gates，并重新建立 node/alarm 证据转移的 exact-path/digest 证明。
3. 只有 fixture 106/106、dispatch 3/3、禁入零命中、18/18 图与矩阵全关闭后，Task 9 才能改为 `PASSED`。

本轮未升版、未打 tag、未 push、未部署、未访问 1 号机、未执行真实控制/通知/策略发布/外部 HTTP。
