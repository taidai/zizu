# 自足IOT 红金亮银首批交付验收

日期：2026-09-07。目标版本：1.0.3；Schema：062（没有新迁移）。

## 范围

本批集成 D0 + R1 + E + A1：红金亮银应用外观，运行/工程导航与原权限，真实 L2 运行首页，节点→L0→L1→L2 配置操作，以及既有告警、调度、系统工具的触控适配。数据仍走既有提交帧，控制仍走原统一入口。本地 Demo 不作为生产数据源。

R2 独立设备卡片、A2 告警十条表及通用原生 JDM 编辑器已独立实现并经代码复审，但尚未合入本批，不能称已上线。

## 本机证据

| 检查 | 结果与边界 |
| --- | --- |
| `scripts/verify_delivery.py` | 后端 705 项：430 通过、275 跳过；脚本测试、前端 135 项、生产构建均 exit 0。脚本未传现场地址，因此总结果诚实保持 INCOMPLETE，而非现场通过。 |
| 正式前端构建 | 1.0.3，8203 模块，1分55秒；既有大包提示仍存在。 |
| 节点真实主干 | 7/7，2.7分钟；实际 FastAPI、独立 PostgreSQL、loopback Neuron/MQTT。覆盖 CRUD、52 点导入、10/20 分页、L0 实时/历史、L1 发布/模板版本/停用恢复/补单位、L2 实时/历史/来源、BIT 原值0/1/2、告警选择及试算、删除/退役。临时资源完成清理。 |
| 调度真实纵向 | 3/3，1.4分钟；实际数据库/JDM/控制意图与回读，只有协议写端使用 RecordingProtocolDispatcher。未写现场设备。 |
| 调度隔离 UI | 12 项以首9项及末3项定向复跑全部通过；保存、试算、发布/启停与阻断展示。实时握手票据不计作配置写入。 |
| 应用触控与并发拒绝 | 4/4，27.5秒；1280×800、1024×768 告警/调度/工具可达、打开不写配置，两种 stale 拒绝清旧试算。 |
| 首批失败边界 | 壳/首页/加工恢复 13/13；目录503显式失败、WebSocket断开不假GOOD、未知提交保留原重试身份。 |
| 发布后关闭弹窗 | 真实React组件4/4：已发布但父刷新未结束可关闭；在途/未知仍保护，未发布修改仍确认。 |
| 测试基础设施 | 本机数据库与进程边界5/5；夹具专用17/17。统一用数据库时钟领取本机意图，避免WSL与Windows时钟偏移导致假失败。 |

浏览器产物保留在被忽略的 `frontend/test-results/tablet-node-v103-final-2`、`tablet-dispatch-real-clock`、`tablet-dispatch-ui-final`、`tablet-dispatch-ui-last-three`、`tablet-applications-v103-final`、`tablet-first-release-recovery-green-2`。组件关闭回归可运行 `node --test src/components/data-trunk/PointProcessingTemplateManager.browser.mjs`，它需要已有 Playwright Chromium，与纯 Node 模型测试分开。

独立首批审查发现的目录失败/未知计划编辑边界已修复并复审 Ready；最后的发布后关闭小修复也已限定复审 Ready。没有增加依赖、放宽质量/时效/控制门禁或调整现场配置。

## 发布准备与未完成项

1号机只读预检确认当前仍为 1.0.2、固定 ARM64 镜像、健康、Schema062；启用策略、控制所有权、在途意图/命令均为0。根分区约2.88GiB余量，拉镜像前继续检查，禁止以清现场数据腾空间。

当次配置与关键控制记录已备份到现场私有发布目录，并在本机新隔离库恢复60张表、重验102个外键、比对配置修订及全部安全计数。6条历史设备模板仅归档，不复建已删除的表；历史遥测不在配置备份范围。恢复库未启动应用或控制任务。私有凭据、现场拓扑和完整备份不提交Git。

当前尚待：CI固定制品、仅替换1号机应用、部署后无头只读主干及可见Browser抽查。未完成这些步骤前，现场交付结论为 INCOMPLETE。
