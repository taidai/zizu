# v0.8.1 告警正式修订恢复修复

2026-09-04，用户明确要求在1号机网络恢复后部署。

## 发布

- 源码/tag：`755cdaff3735e8d32ada9ec82b95a4399fb94201` / `v0.8.1`。
- GitHub Actions `33828298045` 成功，发布清单经 `release_preflight.py` 验证。
- ARM64固定摘要：`ghcr.io/taidai/zizu@sha256:d49feb18fd55ba4008b02dcf05e54276e2f1a418dd54945a25ee514ab172f656`。
- Schema 060不变；仅重建应用容器。TimescaleDB与NanoMQ未重启。
- 应用容器healthy、restart 0，保留host网络及`/dev/mqueue` tmpfs；公网存活接口返回0.8.1。

## 变更

- 告警规则停用后再启用，只恢复最近正式应用的非空修订，不再把最后保存的草稿静默发布。
- 正式修订按每个绑定实体的已应用计划确定；证据缺失或实体间版本不一致时禁止一键启用。
- 规则列表区分正式条件与未发布草稿，并解释同一未恢复事件不会因重复采样、确认或启停重复通知。
- 旧活动事件继续按发生时冻结的定义恢复；本次部署不改事件、规则、设备值或通知任务。

## 验证

- 本地：后端521项退出0（193项环境型PostgreSQL测试跳过）；scripts 56/56、前端mjs 88/88、生产构建通过。
- v0.8.1生产包告警专项18/18通过，包括正式修订恢复、刷新后恢复、缺证据禁用、HTTP选项和请求体光标。
- 1号机PostgreSQL对新增`DISTINCT ON ... DESC NULLS LAST`查询执行`EXPLAIN`成功；未在生产库构造历史夹具。
- 现场无头浏览器只读走通节点→91/91 L0→L1→2个L2→告警；正式修订条件提示和HTTP选项可见。
  脚本退出0，没有配置写入、规则发布、设备控制、测试通知或历史补发。
- 部署前后配置修订、告警定义/current、HTTP配置及绑定的行数和内容摘要完全一致，Schema保持060。
- 部署后60秒抽样：18个COMPLETE、0个FAILED、1个在途帧，outbox待发布0；应用日志未发现ERROR/Traceback。

## 备份与回滚

- `/opt/zizu-release-test-0.5.0/backups/v0.8.1-pre/`保存切换前release.env、Compose及告警数据。
- `alarm-data.dump`为5,526,624字节、权限600，SHA-256
  `80b0ab07cc8a8ac2a9802a0cdcfa83e04f9c118376a757530aa3b83750a37462`；已由容器内`pg_restore -l`完整读取。
- 软件回滚使用目录内旧release.env重新创建backend；不回滚数据库、不重新启用旧规则。
- 根分区可用3.2G，`/userdata`可用2.6G。未启动Caddy/TLS，也未清理旧镜像或业务数据。

## 边界

本次证明代码发布、只读主干和正式修订界面正常；没有制造真实故障，因此不把它称为一次新的故障HTTP送达验收。
两个历史活动告警仍由现场值驱动恢复，不能通过启停规则伪造恢复或重复通知。
