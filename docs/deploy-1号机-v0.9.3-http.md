# 1 号机 v0.9.3 发布记录

## 本次交付

修复统一控制的 Neuron 回执校验：只有 JSON 对象中明确的整数 `error: 0` 才算接口已受理。空白、非对象、缺字段、`null`、布尔值或浮点零均进入既有下发失败路径。接口受理仍不等于控制成功，必须取得新的 committed L2 回读。没有修改回读不符、超时、失败锁或安全边界的语义，也不据此认定已查明此前真实 PCS 回读不符的原因。

- 源码：`c338d1ca1c6a57782622cc219fcc26ad620f9763`，标签 `v0.9.3`，Schema `062`。
- [镜像构建记录](https://github.com/taidai/zizu/actions/runs/33990282511)：成功。
- ARM64：`ghcr.io/taidai/zizu@sha256:b43d8075e9b973c6376667954eff459880d893c4a6d86708fad21c5684156086`。
- 没有新依赖、数据库迁移或前端业务变更。

## 备份与恢复证据

本次配置和部署文件备份位于 `/opt/zizu-release-test-0.5.0/backups/v0.9.3-pre-20260905T203033Z`。原配置转储 SHA-256：`0e943ed501674360259200750c519dde4292694bdff9458887b0065e0856394d`。

另补充配置、策略、控制和审计记录的一致只读快照；在独立本机数据库中加载当前 Schema，恢复 60 张表，逐表比较行数，并重新验证 102 项外键。已验证的 `critical-snapshot-v2.json` 与恢复结果一并归档到上述备份目录，快照 SHA-256：`e11ad90e8698f41511742b2f35dc90bc51c247929fd619387f9c5d75778694e6`。

恢复验证发现并处理了两点：

- 现场遗留的旧规则模板表不属于当前 Schema，其记录保留在备份中，没有为恢复而重建废弃功能。
- 超级表不能只导出父表；本次使用 `COPY (SELECT * FROM ...)` 包含分块数据，并核对导出、恢复行数。第一次漏分块的快照保留为失败证据，不作为有效恢复备份。

这是**关键配置与记录的隔离恢复验证，不是全量遥测或整站灾难恢复验收**。未恢复 L0/L2 历史，隔离数据库未启动应用或连接设备；生产数据没有被恢复或清除。私有数据和恢复脚本不进入公开仓库。

## 部署结果

2026-09-06 04:47（北京时间）后端替换完成，健康接口版本 `0.9.3`，容器 healthy、重启计数 0，固定镜像摘要匹配。仅重建 backend，保留 host 网络、`/dev/mqueue` tmpfs、挂载、权限与重启策略；Compose/runtime 文件与部署前备份一致。数据库与 MQTT 容器身份、启动时间、重启计数不变，未触发回退，未启动 Caddy/TLS。

本轮没有数据库参数实验、磁盘清理、策略启用、清除旧失败锁或设备控制。

后续只读检查确认 Schema 为 `062`，容器内控制模块与本地已验证源码摘要相同；自部署起扫描的 139 行日志中 ERROR/CRITICAL/Traceback 匹配数为 0。该结果只覆盖检查时点前的日志。安全复核中活动策略、所有权、待执行意图和在途控制均为 0，历史控制命令数未增长。

## 自动验证

- 本轮完整后端：645 项，389 通过、256 条件跳过、0 失败；跳过项不计作通过。
- 脚本测试 56 项、前端测试 106 项通过；前端生产构建成功，保留已有大分块警告。
- 修复专项此前经过真实失败复现、最小修复和独立审查；57 项相关测试通过。调度无头夹具测试复验 8/8 通过，首轮首页加载超时证据保留。夹具不代表现场实际控制。
- 本地聚合验证当时未传现场地址，因此报告 `INCOMPLETE`，缺项为现场只读检查；现场浏览器结果另列，不把该状态写成完整交付通过。

## 现场无头浏览器复验

主干六个只读视角通过：节点、L0 实时与刷新、L0 历史、L1 已生效加工及模板入口、L2 历史与来源、告警规则入口。导航约 43 秒，含最后健康查询约 49 秒；页面异常 0、越界业务写入 0。健康显示 `0.9.3`、数据库/MQTT/Neuron connected、Pipeline RUNNING，当前窗口解析和入库错误为 0。这不是 CRUD 全套验收；健康中的 `release_lock` 仍显示 unavailable，本轮未另行修复该项。

调度页面保存原停用草稿并试算：HTTP 200、业务 `EVALUATED`、同一帧且配置修订一致。JDM、绑定、发布版本、停用状态及旧失败锁保持不变；只有草稿保存与无设备副作用的试算请求，没有启用、清锁或控制命令。主线程查看了现场截图，L2 在截图时可用；这一次通过不覆盖此前持续时效失败。

## 回退

回退制品为 v0.9.2：`ghcr.io/taidai/zizu@sha256:a561e3c9ad31b3430cc61114d1d01326276876f22887ef62f6b15ffd246b0a09`。先核对没有活动控制和并发部署，再恢复本次备份的 `release.env`，使用既有 Compose 仅重建 backend：

```sh
cd /opt/zizu-release-test-0.5.0
sudo cp -- backups/v0.9.3-pre-20260905T203033Z/release.env release.env
sudo docker compose -p zizu-release-test --env-file release.env -f docker-compose.test.e606.yml up -d --no-build --force-recreate --no-deps --wait --wait-timeout 100 backend
```

核对旧版本健康后结束；本次无 Schema 变化，镜像回退不应覆盖现场数据库和用户新配置。

## 调度总目标

完整调度仍为 **INCOMPLETE**。持续数据时效与真实 PCS 写后回读闭环尚未验收通过。保持现有策略停用和安全门槛，不能用一次试算、页面走通或本次回执修复替代实际闭环证据。
