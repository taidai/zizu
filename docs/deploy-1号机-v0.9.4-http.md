# 1 号机 v0.9.4 发布记录

## 修复范围

统一控制现在以设备适配器成功返回后的时刻记录 `dispatched_at` 和下发事件。请求处理期间、接口受理前产生的旧观测，不能用于判定回读成功或不一致。创建时间、原超时期限、冷却、重试、终态规则和安全保护保持不变；接口受理不代表设备完成。

源码标签 `v0.9.4` 对应 `61d49d76f983e0587ded1b71e083b72d7c5f6f93`，包含修复 `ef38f2b`。Schema 仍为 `062`，没有新增依赖、数据迁移或前端业务改动。固定镜像由[本次构建](https://github.com/taidai/zizu/actions/runs/34000275120)的 `release.json` 提供。

构建已成功（4分17秒），发布清单校验通过。ARM64固定镜像：`ghcr.io/taidai/zizu@sha256:45babfe419ccb04e9d057ef649b2f8e14fabc07eb7f65ac6b680669030812665`。

## 发布前验证

- 后端648项：392通过、256条件跳过、0失败（36.591秒）。跳过项不计作通过。
- 脚本56项、前端106项通过；前端生产构建成功，保留已有大分块警告。
- 修复前已证明三个新回归场景失败；修复后专项34项通过，独立审查无阻断。该证据不代表真实 PCS 控制闭环通过。
- 本地聚合报告为 `INCOMPLETE`，原因是执行时尚未部署新版本，未传现场地址；现场验证另行记录。

## 备份与回退

发布前备份：`/opt/zizu-release-test-0.5.0/backups/v0.9.4-pre-20260906T000012Z`。52张配置表的转储已校验 SHA-256：`d08d23b854cf0b56e684827bb692af8d1db98dd3f285d29a454445d5953dd254`。

额外的一致只读快照覆盖关键配置、策略、控制和审计记录；在新建的隔离本机数据库中恢复60张当前表，逐表核对行数并验证102项外键。已移除的旧规则模板表仅归档，不重建。快照 SHA-256：`d059878b3bb9cd2a0d3d12423f4a4860fd07a0bd997e024f90fb97d5bf908ca8`。

此验证不包含全量L0/L2历史，也不是整站灾难恢复；隔离库没有启动应用或连接设备。现场数据库未执行恢复或清理。

回退使用原 v0.9.3 镜像：`ghcr.io/taidai/zizu@sha256:b43d8075e9b973c6376667954eff459880d893c4a6d86708fad21c5684156086`。确认没有活动控制或并发部署后，恢复本次备份中的 `release.env`，仅重建 backend：

```sh
cd /opt/zizu-release-test-0.5.0
sudo cp -- backups/v0.9.4-pre-20260906T000012Z/release.env release.env
sudo docker compose -p zizu-release-test --env-file release.env -f docker-compose.test.e606.yml up -d --no-build --force-recreate --no-deps --wait --wait-timeout 100 backend
```

本次无Schema变更，镜像回退不应覆盖现场数据库和新配置。

## 部署状态

2026-09-06 08:12（北京时间）切换完成，后端 `healthy`、重启计数0，健康接口版本 `0.9.4`，固定摘要匹配。仅重建backend；数据库/MQTT容器身份、启动时间和重启计数不变，Compose/runtime、host网络、`/dev/mqueue` tmpfs、挂载与权限配置保持原样。未触发回退，未启动Caddy/TLS或执行存储调整。

部署后确认首页和匿名存活接口均为HTTP 200；Schema `062`，容器内控制模块SHA-256与本地已验证源码一致。检查时自部署起的33行日志中，ERROR/CRITICAL/Traceback匹配数为0，结论仅覆盖该时点。

无头浏览器沿真实节点、L0实时/刷新/历史、L1加工与模板、L2历史与来源、告警规则入口完成六个只读视角检查，页面异常0、越界业务写入0。导航55.964秒，包含最后健康查询62.612秒；主线程已查看现场截图。这是页面操作核验，不是CRUD全套或真实设备控制验收。

现场健康显示 `0.9.4`、数据库/MQTT/Neuron connected、Pipeline RUNNING，当前窗口解析与入库错误为0。`release_lock`仍为unavailable，截图中L2仍显示超时及上次值，不能把页面通过或组件连接正常等同于数据时效通过。

调度页面保存原停用草稿并试算，HTTP 200，但业务结果为 `BLOCKED / L2_INPUT_STALE`。页面正确显示数据过期阻断，未形成设备动作；前后JDM、绑定、配置基线、发布版本、停用状态和旧失败锁保持不变。最终安全复核：启用策略、输出所有权、待执行意图、在途命令均为0，历史控制命令总数未增加。

完整调度交付仍为 `INCOMPLETE`：持续数据时效与真实 PCS 新 L2 回读闭环尚未验收通过。本次不启用策略、不清除旧失败锁、不下发真实控制，也不据该时序漏洞推断历史失败的唯一根因。
