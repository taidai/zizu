# v1.0.9 运行总览发布记录

日期：2026-09-09。源码：`861cdac1d5d8b8a71b4a1b6d4bf516c7e14256ff`。

## 制品与变更

- 发布分支 `release/v1.0.9`，标签 `v1.0.9`。
- [GitHub 构建记录](https://github.com/taidai/zizu/actions/runs/34289709330)成功；版本1.0.9、Schema065。
- ARM64引用：`ghcr.io/taidai/zizu@sha256:2ab41aa7247e693fd798c97adc72c93903b62d3cb30ab2665fcf4f9d2cdca5b3`。
- 运行镜像ID：`sha256:bbe31cbbac2721472a2e3b0b3e71bf118f128b32bad9a1284641848a0367c2a9`。
- 运行总览采用已确认Demo的拟物SVG、四指标卡和五能流节点，新增独立负荷绑定。详见[功能说明](workbench-demo-overview.md)。

## 部署结果

- `DEPLOYED_HEALTHY`：仅替换应用容器，Schema065迁移成功；保留host网络与`/dev/mqueue` tmpfs。
- 数据库、MQTT及Neuron进程未重启；原首页绑定摘要、配置修订、控制记录数量不变。未下发控制，不修改现场指标绑定，不启用Caddy/TLS。
- 配置dump与有界配置/控制快照已备份，隔离恢复到065验证60表、102外键通过；不是全量遥测恢复验证。临时恢复容器与现场上传包已删除，备份保留。
- 旧v1.0.8镜像及部署配置保留。需要回退时先核对065及负荷绑定使用情况，不能盲目降级数据库。

## 验证与边界

- 后端780项：退出0，286项条件跳过；脚本56项通过；前端模型181项通过；生产构建成功（既有大包警告）。
- 本地生产包无头工作台14/14通过，55.8秒；真实PG负荷迁移/独立绑定专项1/1通过，涵盖迁移重放。
- 现场无头首次读取目录超时，证据保留；未改产品或放宽超时，第二次10项主干只读检查通过，采集前进、业务写入0、页面错误0、API错误0。
- 1024×768、1280×800现场截图已人工查看，四卡、五节点及独立负荷配置入口可见。
- 可见Browser插件选择现有标签页连续超时，未宣称其抽查通过。部署与无头检查通过；若按完整验收清单要求可见Browser，剩余该项为`INCOMPLETE`。
- 截图中部分指标显示最后值而非当前值，充电及负荷尚未配置。保留质量与缺失提示；本轮不能宣称现场所有指标已配置或所有当前数据均健康。

私有部署回执、备份、浏览器结果与现场截图仅保存在本机发布制品目录，不进入Git。
