# 1 号机 v0.9.2 发布记录

## 制品

- 源码：`688f57e006846011a486337c7920120c4055f5f5`，标签 `v0.9.2`，Schema `062`。
- [镜像构建记录](https://github.com/taidai/zizu/actions/runs/33972995099)：成功。
- ARM64：`ghcr.io/taidai/zizu@sha256:a561e3c9ad31b3430cc61114d1d01326276876f22887ef62f6b15ffd246b0a09`。
- 内容：减少事务 B 旧 L0 基线的重复物化。详见 [专项验证](reviews/2026-09-05-v0.9.2-l0-baseline.md)。不改变数据库 Schema 或控制语义。

## 部署前保护

配置备份：`/opt/zizu-release-test-0.5.0/backups/v0.9.2-pre-20260905T144946Z`。
包含原 Compose、release.env、runtime.env 和 52 张配置表；备份 672,924 字节，SHA-256 `465082c3756d5c24b2d5399026d6e74bc0622c4da083fac1ebedc745d1b754a5`。已验证备份目录可列出，**不是全量遥测备份，也没有执行隔离恢复**。

本次保留上一轮已修正的 `DB_POOL_MIN=5 / DB_POOL_MAX=5`。runtime.env 期望摘要为 `3ad289b45979b843363f2dae1d334433831ec08261f3b6ccdfffd3f272886af6`；旧发布脚本中此前的 runtime 摘要不能直接复用。

只替换 backend，保留 `network_mode: host`、`/dev/mqueue` tmpfs、挂载、权限和重启策略。数据库、MQTT 与 Neuron 不重启；不启动 Caddy、不申请 TLS。

回退制品为原 v0.9.1：`ghcr.io/taidai/zizu@sha256:87aaa1273230e1ba011720f2b3e06c73b7fac2efa55954935ef903c5f991cc64`。健康或配置校验失败时，恢复本次备份的 release.env 并按原 Compose 仅重建 backend；不回退现场数据与用户新配置。

## 当前验收边界

部署前相同 16 秒低负载探针：64 次实体检查中 1 次超过 10 秒，最大 10.36 秒，仍未达到持续时效要求。短样本不能代表长期稳定。

2026-09-05 23:20（北京时间）已部署完成，健康检查返回 `0.9.2`，容器重启次数 0。固定 ARM64 摘要验证通过；Compose、runtime 和宿主机网络/tmpfs/权限保持一致，数据库与 MQTT 容器及启动时间未变。未触发回退。

无头浏览器现场只读主干复验通过：真实 PCS 节点、L0 实时值/刷新/历史、L1 当前加工与模板、L2 历史与来源、告警规则入口；约 38 秒，页面异常 0、越界写入 0。这不是现场 CRUD 全套验收。

调度页面保存现有停用策略草稿并试算，HTTP 200，无页面异常；但业务结果为 **BLOCKED / L2_INPUT_STALE**，不是调度通过。配置基准仍为 776，JDM 内容、绑定、发布版本、停用状态与旧失败锁均未变化，未向设备下发控制。

时效仍未通过：启动后并行页面验证时，16 秒内 64 次检查有 31 次超过 10 秒，最大 15.51 秒；页面测试结束后独立复测，64 次检查有 18 次超过 10 秒，最大 13.79 秒。短样本且运行负载不同，不以此前样本计算优化百分比，也不声称持续稳定。

后续只读证据显示：相邻帧拍摄间隔多为 3–4 秒，另有 10 秒间隔；PCS/SOC 单次来源到平台接收仅约 12–15 毫秒。机器负载约 5.6，应用与 Neuron 都有较高 CPU 消耗，内存仍充足。这些用于定位处理链与调度延迟，不足以单独归因某条 SQL、CPU 或硬件。

真实调度仍未交付：除持续新鲜度外，原 PCS 写入后回读未改变的故障尚未解决。保持策略停用、旧失败锁保留，不放宽数据时效和功率边界；先复核整条采集/处理/发布链的耗时，再决定下一次聚焦修复。
