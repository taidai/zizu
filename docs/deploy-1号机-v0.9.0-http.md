# 1号机 v0.9.0 数据帧查询修复

日期：2026-09-05。20:32:57（北京时间）已切换为v0.9.0，应用healthy；主干功能7/7通过，性能与调度正向验收仍未通过。

## 版本与边界

- 源码 `368f9b7edf08def4827e62083815572b614c946d`，标签 `v0.9.0`；开发分支已推送，main未移动。
- [构建33965717617](https://github.com/taidai/zizu/actions/runs/33965717617)成功；发布清单及Schema062校验通过。
- ARM64固定镜像：`ghcr.io/taidai/zizu@sha256:daee5deb1f9b0185682ba3d731ae3f7675e1c005eb5a434dd6a51b4acc95d81a`。
- 只修复事务B生成帧通知时重复扫描L2历史的问题。查询改用同事务已经推进的latest，严格绑定实体、事件和帧；质量及来源仍取本帧事实。详见[性能证据与边界](reviews/2026-09-05-frame-outbox-publication-performance.md)。
- 不修改Schema、采样、帧并发、同步提交、10秒新鲜度、设备限值或策略；不重启数据库、NanoMQ、Neuron，不启用Caddy/TLS。

## 发布前验证

- 后端633项：387通过、246条件跳过；新真实PG专项4/4及原数据帧PG28/28另行运行通过。
- scripts56/56、前端106/106、生产构建通过，保留已有大chunk提示。
- 本机v0.9.0无头主干7/7、46.1秒，run `frame_latest_201745`，真实平台API与本机模拟设备；不是现场性能结果。
- 只暂存本次8个代码/测试/版本/证据文件。之前累积的handoff、私有产物和无关文档未提交。

## 备份与回退

- 配置备份：`/opt/zizu-release-test-0.5.0/backups/v0.9.0-pre-20260905T122016Z`，52表、649168字节。
- SHA256：`1503dccd76f14fe10aa235fba84e887dd3a704c8c2f97c5c1f1ddac5c854148f`，pg_restore目录可读，包含Compose/runtime/release副本。仅配置表备份，不是全量遥测备份，未做隔离恢复。
- 回退镜像：`ghcr.io/taidai/zizu@sha256:27fb28e6f5c709fda61c020c11f38da16f1897791a4d97c80aa7208eff12125f`。
- 只修改release.env中平台镜像与版本，使用既有Compose的backend-only替换命令；host网络、`/dev/mqueue` tmpfs及其他容器配置保持一致。切换异常时恢复备份release.env并重建backend，不恢复或清空数据库。
- 切换前策略启用数、所有权、待发/处理中意图、活动命令均为0，历史命令3条。所有后续验证保持只读调度、不清锁、不写真实设备、不发送真实HTTP通知。

## 现场验收

应用容器`92a01d4d4bc1`，restart0。Compose/runtime哈希、数据库及MQTT容器ID/启动时间/重启次数不变，host网络和`/dev/mqueue` tmpfs保持一致，无回退。

- 原现场无头主干`v090_node_203338`：7/7、0失败/跳过、562.811秒，正常退出并清理本次临时资源。测试备用点及其历史永久删除、临时节点/模板退役、临时Neuron节点清理；业务数据与真实设备参数保留。超过5分钟目标，性能门槛未通过。
- 发布后轻量只读测量16.170秒，32轮/64项实体检查，最大年龄13.344秒、最小5.153秒，33项超过10秒。两输入仍存在过期，不能宣称调度就绪；不同负载和采样间隔不支持把前后最大值之差当作提速比例。
- 较重分段探针退出1、未得到有效结果，已保留失败；上述轻量探针仅查询两个指定L2，未改安全期限或数据库参数。
- 可见CUA建页/读取两次超时，不能声称可见插件抽查通过。主线程查看了无头截图；无头主干功能证据独立有效。
- 首次只读试算UI检查`v090_simulate_204706`失败：现有“试算”按钮先调用saveDraft，本次只读测试保护阻止该请求，页面显示Failed to fetch；不是服务器simulate响应失败。没有重复保存草稿来刷通过。改查只读simulate API，结果另记，不能冒充UI试算通过。

最终安全核对：应用仍同一新容器healthy/restart0，策略启用/所有权/活动意图/活动命令均0，历史命令仍3；PCS值156.8及原安全限值不变。根盘3.2GB、数据盘3.0GB可用。部署未回退，未控制或外发通知。

只读接口复验`v090_simulate_205006`：HTTP200，`BLOCKED / L2_CONFIGURATION_MISMATCH`，两输入同帧308968/配置748，SOC99%、PCS156.8kW，零拟议意图。节点验收推进了全站配置修订，旧策略没有被自动保存或发布；draft/published/故障锁/停用状态逐字段保持不变。页面读取无pageerror，主线程已看截图；这是接口与只读页面检查，不是UI“保存再试算”闭环通过。

完整health为ok/v0.9.0/RUNNING，DB/MQTT/Neuron connected，解析与写入错误均0；release_lock仍报告unavailable，不宣称完整发布锁验收。只读测试会话正常退出撤销。

结论：发布成功，主干功能保持可用，局部SQL问题已修；整体新鲜度、现场5分钟性能门槛及调度正向结果仍未达标。下一步优先减少帧处理的重复工作，随后按现有显式保存流程刷新策略草稿并复验；不改安全期限、不新增引擎、不启用或解锁真实控制。
