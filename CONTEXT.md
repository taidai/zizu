# ZiZu Platform

ZiZu 是面向工业控制系统交付的开源物联网低代码平台。平台通过可复用的配置资产连接现场设备语义与运行应用，光储充 EMS 是首个参考交付。

## Language

**实施工程师**:
使用 ZiZu 配置、验证并交付工业控制系统的系统集成商或项目实施人员。
_Avoid_: 普通用户、开发者

**业主操作员**:
接收已交付系统并负责日常监控、告警确认和授权控制的现场人员。
_Avoid_: 实施人员、管理员

**平台管理员**:
负责平台安装、解决方案包生命周期、用户和系统级安全配置的人员。
_Avoid_: 实施工程师、超级用户

**参考交付**:
用真实场景证明平台交付方法、配置资产和运行能力的完整示范系统；光储充 EMS 是首个参考交付。
_Avoid_: Demo、示例页面

**配置资产**:
可导入、复用、校验和版本化的设备、实体、告警、策略及页面配置集合。
_Avoid_: 客户参数、源码定制

**解决方案包**:
面向一种工业控制场景发布的版本化配置资产集合，包含交付所需的配置、参数契约和验收清单。
_Avoid_: 安装包、代码分支、零散模板

**解决方案清单**:
解决方案包根目录中的声明式入口，标识 Schema、包版本、资产引用、兼容范围和升级信息。
_Avoid_: 数据库备份、部署脚本

**参数契约**:
解决方案包对站点差异的强类型声明，定义名称、类型、单位、必填性、默认值、范围和敏感性。
_Avoid_: 环境变量列表、自由表单

**站点配置**:
某个实际项目对配置资产的实例化结果，包含设备拓扑、点位映射和运行参数，但不包含凭据。
_Avoid_: 平台源码、设备模板

**数据主干**:
挂载在物理节点树上的 L0 原始点位、L1 点位加工和 L2 全局实体共同形成的平台核心数据链；它把现场协议事实加工为上层应用稳定消费的业务事实。
_Avoid_: 五层节点树、虚拟点位流水线、功能域编号

**L0 原始点位**:
协议网关从某台实际设备采集的品牌相关原始数据点，具有源键、线协议类型、工程值类型、单位、质量和时间戳；它只供点位诊断和本节点 L1 点位加工使用。
_Avoid_: 全局实体、业务指标、跨节点输入

**L1 点位加工**:
挂载在输出所属节点上的版本化加工配置，通过品牌字段映射、单位归一、状态与故障解析、多点组合及强类型公式，把本节点 L0 或其他节点已标准化的 L2 输入加工为 L2 全局实体；跨节点不得直接读取品牌 L0。
_Avoid_: 点位转换、节点模板、任意脚本、无版本公式

**点位加工模板**:
面向明确设备类别、品牌型号或系统计算场景发布的可复用 L1 配置资产，声明强类型输入、输出实体定义、安全公式、质量和时间策略；安装时把选择器展开为确定绑定，不携带现场凭据或任意代码。
_Avoid_: 点位转换模板、设备实例、站点复制、源码适配器

**L2 全局实体**:
供整个站点的运行工作台、历史、告警、策略、控制和报表共同引用的稳定业务对象；“全局”表示跨上层应用统一可见，不表示跨站或多租户。它由实体定义与站点内实体实例构成，并携带实时值、类型、单位、质量、时间戳和来源证据。
_Avoid_: L0 点位、加工中间值、跨站共享对象

**站点覆盖**:
实施工程师针对某一站点有意偏离解决方案包默认值的配置，并在包升级时受到保留和冲突检测。
_Avoid_: 本地修改、临时修复

**配置式交付**:
实施工程师在不修改平台源码的前提下，通过配置资产和界面完成工业控制系统交付。
_Avoid_: 零代码、定制开发

**单站实例**:
只承载一个实际场站及其运行数据的 ZiZu 部署实例。
_Avoid_: 租户、项目空间

**实体定义**:
L2 全局实体中跨设备品牌与站点复用的标准业务语义，例如 `pcs.active_power`，不代表某台实际设备的当前值。
_Avoid_: 实体实例、L0 原始点位、实时值

**实体实例**:
站点内某个实际对象对实体定义的实例化，例如 PCS-01 的 `pcs.active_power`，是产品界面所称 L2 全局实体的运行身份，也是运行、告警、策略和控制引用的稳定对象。
_Avoid_: 实体副本、带前缀实体、L0 原始点位

**设备实例**:
站点内一台实际设备或一个明确的逻辑设备对象，例如 `PCS-01`；它承载一组实体实例，但不等同于节点、点位或设备模板。
_Avoid_: 节点、设备类型、实体实例

**实体实例槽位**:
解决方案包声明的实例化契约，规定设备类别、数量来源、稳定实例键、命名规则和必需实体定义；安装后产生设备实例及其实体实例。
_Avoid_: 数据库行模板、自由命名前缀

**绑定候选**:
按解决方案包中明确匹配规则找到、等待安装计划判定或实施工程师确认的现场点位，并携带可解释的匹配理由；候选本身不是运行数据源。
_Avoid_: 自动绑定、优先级绑定

**确认绑定**:
实体实例到唯一主数据源的已审查关系；只有确认绑定能进入运行读取，候选数量、优先级或创建顺序不能替代确认。
_Avoid_: 首选绑定、最高优先级来源

**确定性绑定**:
在安装或升级阶段确认的实体实例到唯一主数据源的关系；缺失或歧义都会阻止交付就绪。
_Avoid_: 优先级选择、自动猜测

**交付就绪**:
参考交付同时满足设备接入、监控、历史、分级告警、手自动控制、基础策略、操作审计和部署运维的验收状态。
_Avoid_: 功能完成、页面可见

**交付试验**:
由未参与平台开发的实施工程师在干净环境中，仅使用公开制品、文档、解决方案包和产品界面完成的计时验收。
_Avoid_: 开发验收、功能演示

**交付试验向导**:
面向实施工程师呈现交付步骤、当前证据、阻断原因和下一动作的产品对象；它关联既有领域行为的结果，但不代替控制、策略、告警或授权模块执行动作。
_Avoid_: 通用工作流引擎、验收脚本、万能操作入口

**证据票据**:
由平台签发、短期有效且不透明的试验关联凭据，用于把一次正常产品操作产生的证据归属到特定交付试验及验收项；它不授予权限，也不放宽限值、联锁、冷却或二次确认。
_Avoid_: 授权令牌、命令 ID、审计 ID

**控制命令**:
经统一安全入口执行、可审计并要求写后回读的设备状态变更请求，无论其来源是操作员还是自动策略。
_Avoid_: 写点、RPC、规则动作

**回读确认**:
控制命令下发后，根据声明的目标实体、期望值、容差和超时验证现场状态达到预期的证据。
_Avoid_: 写入成功、接口成功

**告警事件**:
现场观测经过持续时间、迟滞和恢复条件后形成的状态变化；确认只表示操作员知悉，恢复只由现场恢复条件产生。
_Avoid_: 每次采样告警、人工恢复

**告警规则组**:
实施工程师复用的版本化编排对象；每个不可变修订包含一组稳定规则 ID、固定严重度及触发/恢复条件，应用时展开为独立告警定义，运行时不动态引用规则组。
_Avoid_: 可变运行模板、告警等级表

**告警配置计划**:
把批量实体范围与一个告警规则组修订，或一批已明确消歧的旧告警配置，确定性展开后的可审查变更；包含新增、更新、保留、停用候选、阻断项、基准站点版本和计划摘要。生成计划不改变运行态，只有通过摘要与并发复核后才能由同一 apply seam 原子应用。
_Avoid_: 批量写入、临时规则列表

**告警配置验收报告**:
针对一次已应用告警配置生成的不可变机器证据，逐定义关联触发事件、操作员确认审计、现场恢复转换、站点配置版本和内容摘要；验收只观察正常产品链路，不代替告警动作。
_Avoid_: 页面截图、HTTP 200、人工检查表

**验收清单**:
解决方案包携带的机器可执行交付判据集合，用于生成可追溯的交付报告。
_Avoid_: 测试步骤、人工检查表

**EMS 运行工作台**:
业主操作员使用的固定运行界面，其导航、设备分组、关键指标和趋势由解决方案包配置。
_Avoid_: 通用大屏设计器、管理后台

**开源核心**:
公开仓库中的平台代码、配置格式、参考交付资产、部署方法和验收测试。
_Avoid_: 客户现场配置、商业服务

**发布锁定**:
一次可重现交付对平台版本、镜像摘要、解决方案包版本和站点配置版本的完整记录。
_Avoid_: latest 标签、VERSION 文件

**现场私有数据**:
不得进入公开仓库的客户参数、访问凭据、实际设备拓扑和其他项目敏感信息。
_Avoid_: 配置资产、参考交付
